# AEGIS — Plano Diretor de Remediação v1.0.3

**Classificação:** instrumento privado de priorização técnica e continuidade<br>
**Data de atualização:** 2026-09-05<br>
**Horizonte desta revisão:** entrega funcional em 30 dias, até 2026-08-28<br>
**Branch de referência:** `main`<br>
**Commit de referência:** `a4d358c` (PR #63 squash — risco de identidade via Microsoft Entra ID Protection; baseline anterior `50143b8`)<br>
**Última entrega concluída:** **AEGIS-MVP-MICROSOFT-COVERAGE-03** — risco de identidade via Microsoft Entra ID Protection (PR #63, https://github.com/lipeadb-arch/AEGIS/pull/63, squash-merge `a4d358c9d61b0a5b44a2f65ea4502b12cd5a1765`; merged_at 2026-09-05T21:02:46Z; **pai único `50143b820601009ceaf36576f5992e64daac2d71`**; árvore idêntica ao head validado `bd10f918af706691e86af5db076da01c4871938b` — tree `b60e5b8deee4ce56e201e765dcf9f4e57887d597`; 6 commits; `main == origin/main == a4d358c` por fast-forward; branch `feat/mvp-microsoft-coverage-03-identity-risk` removida no remoto). **Nenhum serviço, conector, credencial ou cliente Graph novo:** as duas capacidades rodam **dentro** de `EntraIdKnightCollector`, na mesma operação lógica, com um único token, e KNIGHT e postura leem o **mesmo snapshot** — sem segunda consulta ao Graph. Duas capacidades **independentes**: `riskyUsers` (`IdentityRiskyUser.Read.All`, exige **P2**) e `riskDetections` (`IdentityRiskEvent.Read.All`, exige **P1 ou P2**; com P1 as detecções premium chegam como `generic` e são contadas em `PremiumDetailWithheld`) — **as duas permissões ainda NÃO foram concedidas**. `InsufficientPermission` × `LimitedByLicense` são desfechos **tipados distintos**; parcialidade nunca vira zero (`≥ n` na tela); `hidden` é bucket próprio, distinto de `none`; `unknownFutureValue` nunca conta como resolvido. Janela de 30/7 dias aplicada **em código** com `TimeProvider` injetado, porque a doc oficial só confirma `$filter` e `$select` nesses recursos. **`UserAuthenticationMethod.Read.All` deliberadamente FORA** (N+1 e exposição de PII): a ampliação veio do `$select` do relatório agregado já autorizado por `AuditLog.Read.All`. **Sem migration** — `FactsJson` virou envelope versionado `aegis-identity-evidence-v2` (raiz ARRAY = v1, raiz OBJETO = v2), com leitura compatível do v1 sem inventar zeros. **Somente agregados provider-neutral são persistidos**; teste injeta sentinelas de PII na resposta do Graph e prova que nenhuma atravessa a normalização, o DTO, os logs ou o banco. **Contagens exatas:** backend **1475/1475** (era 1422); `EntraIdentityRiskCollectorTests` **37/37**; `IdentityRiskEvidenceFabricTests` **15/15**; regressão Knight+Identity **114/114**; frontend logic **80/80** (era 55); build **0 erros / 0 avisos**; **sem pending model changes**; CI no head final — `container-smoke` PDF Linux **11/11**, conectores Linux **270/270**, **11 gates**; `migrations-pg` **21/21** em PostgreSQL real (contagens conferidas nos logs). ⚠️ **Falso verde corrigido dentro do PR:** o primeiro CI passou verde sem executar as duas classes novas (os dois jobs filtram por classe); `bd10f91` as incluiu no filtro e o job subiu de **218 para 270**. **Zero alteração em AEGIS Score, AEGIS KNIGHT Score, `TenantControlState`, mapping NIST, ledger ou `EvidenceSignal`** — provado por guardas em SQLite e em PostgreSQL real; os novos agregados **não** entram na fórmula do KNIGHT Score nesta entrega. **Zero deploy** e nenhum ambiente externo ou permissão alterado. ⚠️ **Limitações:** **não houve validação contra tenant real**; enquanto as permissões não forem concedidas, a coleta do Entra passará a reportar `PartialCollection` com o conector `Degraded` — correto e honesto, porém visível na UI; licença real (P1 × P2), incidência de `generic` e volume de paginação só se confirmam ao vivo.<br>
**Entrega anterior imediata:** **AEGIS-MVP-MICROSOFT-COVERAGE-02** — postura de configuração e conformidade de dispositivos via Microsoft Intune (PR #62, https://github.com/lipeadb-arch/AEGIS/pull/62, squash-merge `50143b820601009ceaf36576f5992e64daac2d71`; merged_at 2026-09-04T14:23:16Z; **pai único `7e9b362ac44a10dee948fa8463d2d42a35c3b2c0`**; árvore idêntica ao head validado `b324b75577bf0e378276eaf8a2d4994fd7a8284e` — tree `53a169ac6d18e08c6010786c6ca8bcc89f596c01`; 6 commits; `main == origin/main == 50143b8` por fast-forward; branch `feat/mvp-microsoft-coverage-02-intune-posture` removida local e remotamente). Microsoft Intune integrado como `Microsoft/ConfigAnalyzer`, **quinto serviço independente** do hub Microsoft, com a **mesma credencial** e **sem novo pipeline OAuth**. Duas dimensões **independentes**: (1) políticas/configurações (`DeviceManagementConfiguration.Read.All`, já concedida) e (2) estado efetivo dos dispositivos (`DeviceManagementManagedDevices.Read.All`, **ainda não concedida**) — a ausência da segunda degrada somente a dimensão (2). Estados parciais e falhas **não são convertidos em zero** (viram estado tipado; dimensão sem inventário devolve `null`, nunca `0`). Persistência **tenant-safe** e **reconciliação independente por dimensão**; migration **`DevicePosture_MicrosoftIntune`**. **Contagens exatas:** backend **1422/1422**; frontend logic **55/55**; PostgreSQL **20 testes** (incluindo os 3 `DevicePosturePostgresTests`); conectores Linux **218 testes**; `container-smoke` com **11 gates**; build **sem erros ou avisos**; **sem pending model changes**. **Zero alteração em AEGIS Score, `TenantControlState`, mapping NIST ou `EvidenceSignal`**; **zero deploy** e nenhum ambiente externo alterado. ⚠️ **Limitações:** falta conceder `DeviceManagementManagedDevices.Read.All`; **não houve validação contra tenant Intune real**; o estado efetivo dos dispositivos permanecerá `NotAuthorized` enquanto faltar a permissão; o comportamento real de `assignments` ainda precisa ser confirmado; a correlação com ativos Defender continua pendente por falta de identificador estável preservado; o Google SecOps continua adiado até a credencial ser configurada. **Fila:** (1) validação viva do Intune após a concessão da permissão; (2) **AEGIS-MVP-MICROSOFT-COVERAGE-03 — Identity Risk** (`IdentityRiskyUser.Read.All`, `IdentityRiskEvent.Read.All`, `UserAuthenticationMethod.Read.All` apenas para métodos detalhados); (3) Defender Unified RBAC; (4) comparação Microsoft 365 Activity Feed × Google SecOps; (5) validação real do Google SecOps quando o Service Account JSON estiver disponível; (6) consolidação do dashboard executivo e relatório inteligente.<br>
**Anterior:** **AEGIS-MVP-MICROSOFT-COVERAGE-01** — inventário e exposição de software via Microsoft Defender (PR #61, https://github.com/lipeadb-arch/AEGIS/pull/61, squash-merge `7e9b362ac44a10dee948fa8463d2d42a35c3b2c0`; merged_at 2026-09-03T19:15:16Z; **pai único `a8ad031f60828b9659e5ddc7fe0d95b1c39af8bc`**; árvore idêntica ao head validado `b9264a5a87971969bee477bfa0cd92c0619736df` — tree `3d8165cc87385c823cd9a8088afa52bb7b28fcdd`; **6 commits** `400cfe5`→`0ce1139`→`2572a25`→`f7a0d35`→`5e820cc`→`b9264a5`; branch `feat/mvp-microsoft-coverage-01-software-inventory` removida local e remota; `main == origin/main == 7e9b362` por fast-forward; **nenhum deploy, Neon/Render/tenant Microsoft e permissões do Azure intocados**; checks `migrations-pg` (PostgreSQL 18 real, 17 executados/0 falhas/0 pulados, incluindo `SoftwareInventoryPostgresTests`) + `container-smoke` = success como gate no head `b9264a5`). **STATUS: CONCLUÍDO e mergeado.** A permissão `Software.Read.All`, antes concedida e ociosa, passa a ser consumida pelo conector Defender já existente: **um token e uma fotografia de `/api/machines`** reaproveitados pelas duas dimensões, sobre `GET /api/Software` (`$top`/`$skip`) e `GET /api/machines/SoftwareInventoryByMachine` (`pageSize` + `@odata.nextLink` exclusivamente). Modelo provider-neutral **produto × versão × instalação × ativo** com binding por fonte; **reconciliação latest-snapshot, idempotente e multi-source** (resolução por omissão só em coleta completa; falha/parcial preservam dados; falha de uma dimensão nunca invalida a outra). Migration **`SoftwareInventory_MicrosoftCoverage01`** com **FKs compostas tenant-safe**; `has-pending-model-changes` limpo. Aba **"Software exposto" dentro de Vulnerabilidades** (sem novo item de menu), com KPIs, filtros, ordenação e primeira ação determinísticos — nunca gerados por IA. **ZERO `EvidenceSignal`, ZERO `TenantControlState`, ZERO impacto no AEGIS Score.** A revisão adversarial pré-merge corrigiu três achados: **inclusão real de `SoftwareInventoryPostgresTests` no `migrations-pg`** (o teste nunca havia executado — 16 testes antes, 17 depois), ordem do wipe do tenant demo diante das novas FKs `Restrict`, e acessibilidade das abas. Validações: backend 1360/1360 antes da revisão; revisão focada 78/78, regressão proporcional 86/86, conector 51/51; frontend `test:logic` **42/42** (39 anteriores + 3 novos; o 33/33 relatado antes estava errado por soma incorreta — sem regressão); `ng build` aprovado. ⚠️ **Nenhuma validação contra tenant Microsoft real** — só HTTP simulado contra os campos oficiais documentados. **Próximos trabalhos permanecem como FILA, nenhum iniciado, autorizado ou escolhido:** validação do Google SecOps contra instância real quando houver credencial autorizada; Intune Device Configuration/Compliance (estado efetivo dos dispositivos; futura necessidade de `DeviceManagementManagedDevices.Read.All`); identidade de risco como candidato futuro (`IdentityRiskyUser.Read.All`, `IdentityRiskEvent.Read.All`, `UserAuthenticationMethod.Read.All` somente para análise detalhada de métodos); Defender Unified RBAC; comparação do Microsoft 365 Activity Feed com a telemetria já ingerida pelo Google SecOps; dashboard executivo e relatórios correlacionados após consolidar as fontes.<br>
**Anterior:** **AEGIS-MVP-EVIDENCE-FABRIC-01** — Evidence Fabric de identidade (Microsoft Entra ID) (PR #60, https://github.com/lipeadb-arch/AEGIS/pull/60, squash-merge `a8ad031f60828b9659e5ddc7fe0d95b1c39af8bc`; merged_at 2026-09-03T14:12:27Z; **pai único `6f02f7b3e0215c4dd75e3a8a1bb8abdd63ad0ee9`**; árvore idêntica ao head validado `5f3ab84` — tree `714e6738199693f8ba43caec38c0ce4c29ab82fc`; **6 commits** `5392afe`→`acb4f8e`→`4d9d901`→`635ffd2`→`5731efb`→`5f3ab84`; branch `feat/mvp-evidence-fabric-01` removida local e remota; `main == origin/main == a8ad031` por fast-forward; **nenhum deploy, Neon/Render/tenant Microsoft real intocados**; checks `migrations-pg` (PostgreSQL 18 real, 16 executados/0 falhas/0 pulados, incluindo `IdentityEvidencePostgresTests`) + `container-smoke` = success como gate no head `5f3ab84`). **STATUS: CONCLUÍDO e mergeado.** Uma aquisição real do Entra ID alimenta o snapshot compartilhado (`IdentityEvidenceSnapshot`) consumido pelo AEGIS KNIGHT e pela rota de postura — nunca duas consultas ao Graph; `EntraIdTelemetryProviderStub` retirado do DI de produção. O HUD NIST vivo (`ControlStateDashboardQuery`) reconhece a coleta do KNIGHT em PR.AA-01/PR.AA-03 (fonte + horário) sem alterar `ControlStatus`/pontos — o controle permanece `NotEvaluated`. **GV.RR-01 permanece documental** (token `MANUAL_AUDIT_REQUIRED` intacto). Migration única `IdentityEvidenceFabric` (FK composta tenant-safe, `has-pending-model-changes` limpo). **Persistência é LATEST SNAPSHOT** (upsert idempotente por `(TenantId, ConnectorConfigId)`), **não histórico append-only** — histórico/tendências ficam PENDENTES. Testes: focados 74/74; regressão proporcional 761/761; frontend `test:logic` 39/39. **Nenhuma alteração no AEGIS Score, NIST, ledger ou autoridade da IA; Knight Score permanece distinto do AEGIS Score.** **Próximo pacote: `AEGIS-MVP-MICROSOFT-COVERAGE-01`** (Defender Software Inventory via `Software.Read.All`; Intune Device Configuration/Compliance; permissões mínimas adicionais para estado efetivo dos dispositivos; Defender Unified RBAC via `RoleManagement.Read.Defender`, dependente de API beta; Activity Feed/DLP só após comparar cobertura e duplicidade com o Google SecOps) — **ainda NÃO iniciado**. Depois da consolidação das fontes: dashboard executivo e relatórios correlacionados.<br>
**Anterior:** **AEGIS-MVP-ADMIN-LIFECYCLE-01** — gestão segura de tenants, usuários e conectores (PR #59, https://github.com/lipeadb-arch/AEGIS/pull/59, squash-merge `6f02f7b3e0215c4dd75e3a8a1bb8abdd63ad0ee9`; merged_at 2026-09-03T00:10:44Z; **pai único `c61256b7145344e3af65d1cc64ce839fa8d47684`**; árvore idêntica ao head validado `0c4de8a` — tree `a23505ef03592ecbe989ccb00b1009c4aec2260c`; **6 commits** `d3176ca`→`bc8f510`→`e965862`→`11d3ee7`→`562b5f5`→`0c4de8a`; branch `feat/mvp-admin-lifecycle-01` removida local e remota; **SEM migration**; **nenhum deploy, Neon/Render intocados**; checks `migrations-pg` + `container-smoke` = success como gate no head `0c4de8a`). **STATUS: CONCLUÍDO e mergeado.** Catálogo de tenants para PlatformAdmin (renomeia só o nome, slug imutável), suspensão/reativação sem exclusão física (refresh tokens revogados; **access token já emitido dura até o TTL máximo de 10 min**); usuários por remover/readmitir acesso (identidade e histórico preservados; **mesma limitação honesta de até 10 min** após remoção/redução de privilégio); conectores editáveis/habilitáveis/desabilitáveis/desconectáveis — desabilitar preserva credencial, desconectar elimina `EncryptedSettings`+`IngestionKeyHash` preservando linha e proveniência, **backend recusa habilitar conector desconectado (409)**, reconfiguração reativa a mesma chave natural sem duplicar; **nenhuma alteração no AEGIS Score, NIST, ledger ou autoridade da IA**.<br>
**Anterior:** **AEGIS-MVP-LANGUAGE-02** — linguagem clara e organização executiva de vulnerabilidades, exposições e prioridades (PR #55, https://github.com/lipeadb-arch/AEGIS/pull/55, squash-merge `24e5509cf3ec58ca0b99af2f1222ef86e64d6b67`; baseline anterior `ce6d9893`; `main == origin/main == 24e5509` por fast-forward; **pai único `ce6d98933d4e610a405a8b3f0f5cfd240c07507b`**; árvore idêntica ao head validado `eaf51c1` — tree `9034a40af988eb4800a2a037a82eacc8ab92f98c`; 3 commits `0d48427`→`4465c19`→`eaf51c1`; merge por `lipeadb-arch` em 2026-08-30T01:04:59Z; branch `feat/mvp-language-02-operational-findings` removida local e remota; **SEM migration** — contexto principal permanece em **32 migrations**). **STATUS: CONCLUÍDO e mergeado.** **CONCLUÍDO nesta entrega:** (a) **organização de vulnerabilidades por PROBLEMA/CORREÇÃO** — agrupadas por CVE (ativo×CVE vira detalhe paginado sob demanda, com erro e nova tentativa), com contagens separadas de ativos afetados/abertos/resolvidos e **ciclo de vida calculado no NÍVEL DO GRUPO** (CVE misto = Aberto, jamais resolvido por engano; ocorrência única em "Todas"); produto e KB **derivados só da evidência das observações** (nunca do subtipo do ativo), datas pelas observações reais, e **`VulnerabilityNarrative` como autoridade determinística ÚNICA** de título/severidade/exploit (disponibilidade, nunca "exploração ativa")/impacto/primeira ação (ação por KB → produto → fallback neutro de provedor); (b) **linguagem clara de EXPOSIÇÕES** — exposições `SourceOnly` em moldura pt-BR (categoria + serviço) **sem inventar tradução oficial**, vocabulário determinístico de categorias e ameaças, texto de fonte **sanitizado** e busca também pelos campos de linguagem clara; (c) **Central de Prioridades orientada a "o que corrigir, por que importa, alcance e primeira ação"**, consumindo — como o contexto do Auditor — a MESMA narrativa do backend. **Postura NIST, exposição de CONFIGURAÇÃO e VULNERABILIDADE permanecem indicadores DISTINTOS** (sem score híbrido). **Score, fórmula, pesos e estados determinísticos INALTERADOS; IA permanece CONSULTIVA** (explica/correlaciona/contextualiza/apoia a remediação, sem decidir conformidade, lifecycle, severidade, exploit, alcance ou priorização factual); a interface básica é compreensível **mesmo sem chamar a IA**. **Evidências de conclusão:** build da API **0 erros / 0 avisos**; backend direcionado **46/46**; frontend `npm run test:logic` **24/24**; `ng build` aprovado (só warnings de budget CSS preexistentes); **`migrations-pg` verde — PostgreSQL 18 real, `AEGIS_PG_GATE_REQUIRED=true`, 8 executados / 0 falhas / 0 pulados**, incluindo os **DOIS testes PostgreSQL de `VulnerabilityManagementPostgresTests`** (`Migration_GlobalUniqueness_And_MultiSourceLifecycle_OnRealPostgres` e `Overview_GroupsByCve_MixedLifecycle_ProductFromEvidence_OnRealPostgres` — este comprova no Postgres real: agrupamento por CVE, CVE misto = Aberto, ausência em Resolvidas, ocorrência única em "Todas", produto/KB da evidência, 1ª ação pela KB, busca por CVE antes da paginação); **`container-smoke` verde**; **nenhuma migration nova / `has-pending-model-changes` limpo**. **Nenhum deploy; Neon/Render não tocados.** **Próximo pacote — PRODUTO (nesta ordem): (1)** fundação **provider-neutral do Google SecOps/Chronicle** (SIEM sobre a Chronicle API moderna); **(2)** posteriormente, mapeamento comprovável **Google SecOps → MITRE ATT&CK → NIST CSF 2.0**, **condicionado a evidência determinística suficiente** — **alertas, incidentes ou cobertura MITRE NÃO viram crédito automático de score**.<br>
**Anterior:** **AEGIS-MVP-LANGUAGE-01-CONTROLS** — linguagem clara determinística + controles não avaliados (PR #54, https://github.com/lipeadb-arch/AEGIS/pull/54, squash-merge `ce6d98933d4e610a405a8b3f0f5cfd240c07507b`; baseline anterior `ceb4be26`; `main == origin/main == ce6d9893` por fast-forward; **pai único `ceb4be26`**; árvore idêntica ao head validado `b6cec42` — tree `20bef56f`; 3 commits `06cc4e3`→`771fe72`→`b6cec42`; merge por `lipeadb-arch` em 2026-08-29T14:10:40Z; branch `feat/mvp-language-01-controls` removida local e remota; **SEM migration** — contexto principal permanece em **32 migrations**). **STATUS: CONCLUÍDO e mergeado.** Catálogo AUTORAL, determinístico e provider-neutral em pt-BR (`aegis_control_language.pt-BR.json`, carregado pelo singleton lazy `ControlLanguageCatalog`) cobrindo **exatamente as 106 subcategorias ATIVAS** do NIST CSF 2.0, com **título, resumo, impacto e ação inicial** específicos por controle; a descrição OFICIAL do NIST permanece como **referência secundária** (proveniência `derived`/pt-BR — NÃO é tradução oficial, sem sobrescrever `nist_csf_2_0_catalog.json`). Controles **`NotEvaluated` visíveis nas SEIS funções** (GV/ID/PR/DE/RS/RC) — **Respond com 13 e Recover com 8** controles, mesmo sem avaliação; seguem **FORA do numerador e do denominador** do score (não é 0%, não é NonCompliant; percentual local anulável, headline pela autoridade de `/scoring/workspace`). **Motivo de não-avaliação DETERMINÍSTICO** derivado do `RuleEvidenceType` tipado (TelemetryRequired/DocumentationRequired/BothRequired/Unsupported), com **lacunas genéricas provider-neutral** (identificadores de máquina traduzidos por `sourceLabelOf()`; sem revelar fornecedor sem evidência do tenant; `Unsupported` sem requisito inventado); **títulos específicos também no balanço executivo de pontos cegos** (`buildGapBalance`); catálogo **FAIL-CLOSED em runtime** (`IControlLanguageCatalog.GetRequired`) — código ativo sem redação falha explícita e sanitizada (só o código), nunca silenciosamente incompleto. **Score, fórmula, pesos e estados avaliados INALTERADOS; IA permanece CONSULTIVA** — explica, correlaciona, contextualiza risco e apoia a remediação, sem decidir score, conformidade ou peso. **Evidências de conclusão:** backend direcionado **30/30** (`ControlLanguageCatalogTests`, `ControlStateDashboardQueryTests`, `SignalFreshnessTests`); frontend `npm run test:logic` **20/20**; build da API **0 erros / 0 avisos**; `ng build` aprovado (apenas warnings de budget CSS preexistentes); **`migrations-pg` verde** e **`container-smoke` verde** (ambos `completed/success` no PR #54); **nenhuma migration nova / `has-pending-model-changes` limpo**. **Limitações honestas:** este pacote resolve a linguagem dos CONTROLES — **vulnerabilidades, exposições e prioridades ainda precisam do próximo incremento**; **nenhuma fonte adicional conectada**; **Google SecOps ainda não implementado**; **nenhum deploy nem validação operacional pública** nesta rodada; Neon/Render não tocados. **Próxima ordem — PRODUTO (nesta sequência): (1) `AEGIS-MVP-LANGUAGE-02`** — linguagem clara e organização executiva para vulnerabilidades, exposições de configuração e Central de Prioridades; **(2)** agrupar vulnerabilidades por problema/correção, deixando ativo×CVE para o detalhe; **(3)** títulos claros e tradução/sanitização das recomendações de exposições; **(4)** prioridades respondendo diretamente o que corrigir, por que importa, alcance e primeira ação; **(5)** depois, fundação provider-neutral do **Google SecOps/Chronicle**; **(6)** posteriormente, mapeamentos comprováveis **Google SecOps → MITRE ATT&CK → NIST CSF 2.0**, sem conceder crédito ao score por mera contagem de alertas.<br>
**Anterior:** **AEGIS-MVP-MICROSOFT-HUB-SENTINEL** — conexão Microsoft unificada + Sentinel operacional somente leitura (PR #53, https://github.com/lipeadb-arch/AEGIS/pull/53, squash-merge `ceb4be26ad1947b1f14498e8b95f87806810b989`; baseline anterior `4b7a555`; `main == origin/main == ceb4be26` por fast-forward; **pai único `4b7a555`**; árvore idêntica ao head validado `89fcebc` — tree `694317a`; 4 commits `0da9576`→`0f4d66d`→`cc8b0af`→`89fcebc`; merge por `lipeadb-arch` em 2026-08-29T00:59:18Z; branch `feat/mvp-microsoft-hub-sentinel` removida local e remota; **SEM migration** — contexto principal permanece em **32 migrations**). **STATUS: CONCLUÍDO e mergeado.** Conexão Microsoft ÚNICA na interface (credencial comum informada uma vez, cifrada, write-only e nunca retornada pela API/log) com **fan-out** para `ConnectorConfig` independentes por capacidade — Secure Score, Entra ID / AEGIS KNIGHT, Defender Vulnerability Management e Sentinel mantêm habilitação, intervalo, última sync, status, erro, testar, sincronizar e **isolamento de falha PRÓPRIOS**, sem conector monolítico e sem duplicidade (upsert pela chave natural). `workspaceId` EXCLUSIVO do Sentinel e **validado como GUID ANTES do primeiro upsert** (workspace inválido rejeita a seleção inteira, sem escrita parcial). Sentinel **somente leitura** pela Azure Monitor Log Analytics Query API (KQL FIXA no servidor; `arg_max` por `IncidentNumber` deduplica o histórico; timespan explícito), com **endpoint da consulta `api.loganalytics.azure.com` e audience OAuth `https://api.loganalytics.io/.default` DISTINTOS e oficiais** (sem URL fornecida pelo tenant), **HTTP 400 do endpoint OAuth classificado como falha de autenticação** (sem `error_description`/corpo/segredo/token) e **estado EXPLÍCITO da coleta secundária de `SecurityAlert`** (Available/TableUnavailable/PermissionDenied/Throttled/Timeout/Unavailable/Partial): **zero alertas só quando `Available`**; qualquer outro estado preserva os agregados de incidentes, marca `IsComplete=false` e deixa o conector `Degraded`; a falha PRIMÁRIA marca só o Sentinel como `Failed`. **A autoridade do score e da IA NÃO muda:** a fotografia é CONSULTIVA — não cria `EvidenceSignal`, não altera `TenantControlState`/ledger nem o AEGIS Score, e **nenhum mapping NIST** foi criado por contagem de incidentes/alertas; a IA permanece consultiva. **Evidências de conclusão:** backend direcionado **74/74** (`LogAnalyticsClientTests`, `MicrosoftSentinelConnectorTests`, `SentinelPullTests`, `TenantManagementServiceTests`); frontend `npm run test:logic` **8/8**; builds backend e frontend aprovados; **`migrations-pg` verde — PostgreSQL 18 real, `AEGIS_PG_GATE_REQUIRED=true`, 7 executados / 0 falhas / 0 pulados**; **`container-smoke` verde** (imagem construída, PostgreSQL descartável, DbMigrator antes da API, migrations+seed, prontidão, `/health/ready`+`/health/live` 200, SPA, execução não-root — todos os gates `[PASS]`); **nenhuma migration nova / `has-pending-model-changes` limpo**. **Dependência EXTERNA:** validação contra um **workspace Sentinel REAL** (RBAC de leitura + dados) permanece PENDENTE — transporte/parsing validados por **HTTP SIMULADO**; a fotografia do SIEM ainda **sem persistência histórica nem tela dedicada**; nenhum deploy/Neon/Render tocado. **Próxima ordem — PRODUTO (nesta sequência):** (1) **linguagem clara provider-neutral** — camada determinística de explicação para controles, vulnerabilidades, exposições e prioridades; (2) **Google SecOps/Chronicle** — fundação provider-neutral de SIEM sobre a Chronicle API moderna; (3) **mapeamentos Google SecOps → MITRE ATT&CK → NIST CSF 2.0 / AEGIS comprováveis**, pontuando somente quando houver evidência determinística suficiente.<br>
**Entrega anterior consolidada:** **HARDENING OPERACIONAL DE IA, ENTRA/KNIGHT E DEFENDER — PRs #45–#52** (`main == origin/main == 4b7a555ec72d1a50097f7db26f4c8044ac84d46e`; baseline anterior `bf05741`; oito commits lineares, **sem migration**). **IA:** PR #45 (`2a738944`, https://github.com/lipeadb-arch/AEGIS/pull/45) migrou o adaptador externo de Gemini para Anthropic Messages API, preservando o contrato provider-neutral `ILLMClient`, prompts, roteamento tenant-scoped, allowlist, falhas sanitizadas e score determinístico; o commit registra 1016/1016 testes e nenhum segredo. PR #46 (`42a9f818`, https://github.com/lipeadb-arch/AEGIS/pull/46) adicionou `ExternalEnterprise`, que continua fail-closed por chave + allowlist de slug, e expõe `EnterpriseConfigured` apenas como estado de configuração, não health check; o `render.yaml` declara Anthropic/Claude e a configuração efetiva de painel/secret permanece externa ao Git. **KNIGHT/Graph:** PR #48 (`67041b80`, https://github.com/lipeadb-arch/AEGIS/pull/48) adicionou diagnóstico sanitizado por status HTTP + código Graph + path, sem token, segredo, query, corpo, payload ou PII; a operação real comprovou que o Client Credentials exige permissões **Application**, e após correção + Admin Consent o KNIGHT concluiu a coleta. **Defender:** PR #47 (`d034b43b`, https://github.com/lipeadb-arch/AEGIS/pull/47) introduziu streaming e página padrão 1.000; PR #49 (`108e87de`, https://github.com/lipeadb-arch/AEGIS/pull/49) removeu a leitura global de CVEs e enriquece somente IDs observados em lotes de 40; PR #50 (`d25a5d33`, https://github.com/lipeadb-arch/AEGIS/pull/50) reduziu o índice/deduplicação de machine×CVE; PR #51 (`4e5ae7be`, https://github.com/lipeadb-arch/AEGIS/pull/51) moveu a sincronização longa para background, retorna 202, publica `Syncing` e sanitiza HTML de gateway. Em produção, o fluxo pós-#51 processou **422.683 relações únicas, 0 inválidas**, voltou a `OPERACIONAL`, mas ainda exibiu zero vulnerabilidades, isolando o gargalo na persistência. PR #52 (`4b7a555e`, https://github.com/lipeadb-arch/AEGIS/pull/52) implementou checkpoints/lotes limitados no reconciliador, limpeza do `ChangeTracker`, resolução set-based somente depois de todos os fatos positivos e recuperação idempotente/fail-closed. **Evidência conhecida:** PRs #47–#50 tiveram `container-smoke` verde conforme relatório; o #48 precisou de correção de namespace antes do CI aprovado. **STATUS:** código sincronizado; #45–#52 mergeados. **VALIDAÇÃO OPERACIONAL DO #52 AINDA PENDENTE:** deploy manual da baseline `4b7a555` → UMA sincronização → observar paginação, checkpoints, memória/restarts → confirmar CVEs/exposições e `LastSyncAt`; se a reconciliação concluir e a UI continuar zerada, revisar query/API. O workflow de ownership/estado/SLA volta a ser a próxima entrega de produto após essa estabilização. Postura NIST e vulnerabilidades seguem separadas; IA permanece consultiva e sem autoridade sobre score/lifecycle.<br>
**Anterior:** **AEGIS-MVP-PRIORITIES-01** — Central Operacional de Prioridades / visão unificada de LEITURA (PR #44, squash-merge `bf057415ae24362e232fa207cf6e923a98645602`; baseline anterior `e10cef8`; `main == origin/main == bf05741` por fast-forward; árvore idêntica ao head validado `3e00039` — tree `30053c3`; 2 commits `ac39bc4` (implementação) + `3e00039` (revisão corretiva); merge por `lipeadb-arch` em 2026-08-24T11:49:32Z; **SEM migration** — contexto principal permanece em **32 migrations**). **STATUS: CONCLUÍDO e mergeado.** Primeiro incremento vertical da **visão unificada de LEITURA** (o eixo PRODUTO previsto no ENV-01, agora ENTREGUE): **Central de Prioridades em `/priorities`** + endpoint tenant-scoped read-only **`GET /api/v1/priorities`**, que **COMPÕE** postura NIST, exposições de configuração e vulnerabilidades em ativos numa única leitura reutilizando as queries autoritativas (`IWorkspacePostureQuery` + `IPostureExposureQuery` + `IVulnerabilityQuery`), preservando resumos/itens/ordenação determinística (estado aberto, página 1, teto 5) **sem recálculo**. **DUAS FILAS SEMANTICAMENTE SEPARADAS** (exposições × vulnerabilidades), **SEM score híbrido/agregado** — **a postura NIST e o risco/exposição permanecem indicadores DISTINTOS**. Contrato **PROVIDER-NEUTRAL** (multicloud-ready; cada fila mostra a própria fonte; sem enum/condicional fechado de Microsoft). **IA CONSULTIVA** via `AgentStateService.requestAudit` — **nenhum código/autoridade de IA alterado**. **Evidências:** `PriorityWorkspaceTests` **9/9** (inclui resolução da árvore de DI no composition root real); DI direcionada **15/15**; **frontend `ng build` verde**; **warning de CSS próprio da página nova ELIMINADO** (sem mexer no orçamento global nem silenciar warning); **`container-smoke` verde** ("SMOKE DO CONTAINER OK" — imagem com frontend, PostgreSQL descartável, migrator antes da API, bootstrap, `/health/ready`+`/health/live` 200, SPA, não-root, secret legível); **`migrations-pg` verde — PostgreSQL 18.6 real, `AEGIS_PG_GATE_REQUIRED=true`, 7/0/0**; **nenhuma migration / `has-pending-model-changes` limpo**. **Limitação restante:** o projeto ainda **NÃO possui harness HTTP de integração compartilhado** (`WebApplicationFactory`/`TestServer`) — melhoria **TRANSVERSAL futura que NÃO bloqueia o MVP** (endpoint coberto por composição, filtros, cancelamento, contrato do controller `[Authorize]`/sem-tenant-externo e resolução real de DI). **Nenhum deploy; Neon/Render não tocados.** **Próxima ordem — PRODUTO:** (1) **workflow FUNCIONAL de remediação** sobre os findings existentes — responsável/ownership, estado e prazo/SLA — **sem transformar a IA em autoridade**; (2) **ampliar fontes cloud/on-prem** (Google Cloud/AWS/on-premise) **sem alterar o contrato provider-neutral**. **Próxima ordem — OPERACIONAL (dependência EXTERNA, separada do produto):** deploy manual e medição no Neon **somente quando autorizado e com franquia disponível**. Anterior: **AEGIS-MVP-ENV-01** — medição de ambiente e cobertura por evidência (PR #43, squash-merge `e10cef8500fde2fa9b6b10274143d0917e993be5`; baseline anterior `28e7d26`; `main == origin/main == e10cef8` por fast-forward; árvore idêntica ao head validado `967987e`; 3 commits `6872bf6` (implementação) + `f449a8d` (correções de integridade/UX) + `967987e` (handoffs pós-OPS-01); merge por `lipeadb-arch` em 2026-08-23T17:50:52Z; **SEM migration** — contexto principal permanece em **32 migrations**). **STATUS: CONCLUÍDO e mergeado.** Reposiciona a experiência inicial para **ENVIRONMENT-FIRST** sem alterar metodologia nem fórmula. A projeção única `GET /api/v1/scoring/workspace` passa a expor `EvidenceCoverage` com quatro buckets EXCLUSIVOS — `Telemetry`, `Documentation`, `Both`, `NotAutomated` — cada um com controles/peso elegíveis e avaliados e **cobertura PONDERADA por `MaxScorePoints`**; as somas dos quatro buckets reconciliam EXATAMENTE com o `Overall`. Classificação por autoridade ÚNICA `AegisAssessmentRule.EvidenceType` (sem inferência textual/nome de conector/Função); subcategoria sem regra → `NotAutomated`; enum inválido e QUALQUER duplicidade por `SubcategoryId` (tipo igual ou divergente) falham fail-closed. O Dashboard distingue: nenhuma integração configurada · integrações configuradas porém desabilitadas · conectores sem sincronização · sincronização sem cobertura técnica · medição técnica · coleta parcial · evidência histórica separada da saúde atual; TenantAdmin recebe CTAs administrativos e não administrador recebe orientação sem botão enganoso. **Governança documental preservada** (Document Hub, upload, integrações documentais, entrevistas, análise por IA, evidência literal, reconciliação, cobertura híbrida, teto documental de 50%); documentos apenas REPOSICIONADOS como **complemento dirigido** — a exigência documental NÃO foi eliminada, a quantidade de regras documentais NÃO foi reduzida e a metodologia permanece intacta. Score/fórmula/pesos/catálogo inalterados (cobertura ≠ conformidade); vulnerabilidades seguem FORA do crédito NIST direto (lifecycle intocado); IA CONSULTIVA, sem nova chamada automática ao abrir o Dashboard. **Evidências:** backend local **996/996** (0 falhas, 0 ignorados); direcionados **47/47**; `WorkspacePostureQueryTests` **22/22**; fórmula **12/12**; **PostgreSQL 18.6 real no CI** (`migrations-pg`, `AEGIS_PG_GATE_REQUIRED=true` — sem falso verde) com **7 testes, 0 falhas, 0 pulados**; **`container-smoke` verde** (imagem, Angular+backend compilados na imagem, PostgreSQL descartável, DbMigrator antes da API, migrations+seed, prontidão, `/health/ready`+`/health/live` 200, bootstrap, SPA, não-root); **Angular build aprovado**; **nenhuma migration / `has-pending-model-changes` limpo**. **Limitações honestas:** cobertura por natureza é global do tenant, **ainda SEM recorte por Função NIST**; frontend **sem runner automatizado** (jornada em funções puras, validada por build + tabela de decisão); unicidade por `SubcategoryId` garantida no CONSUMIDOR (fail-closed), **ainda SEM constraint própria no banco** (índice único só em `SubcategoryCode`); **nenhum ambiente real conectado**; **PostgreSQL/Neon público NÃO validado**, **redução de rede NÃO comprovada**, **visão unificada de risco NÃO iniciada**. **Nenhum deploy/Render/Neon/tenant real tocado.** **Próxima ordem — OPERACIONAL:** (1) disponibilidade/renovação da franquia do Neon; (2) deploy manual autorizado; (3) observação do efeito do readiness (transferência/frequência de conexões) e estabilidade de `/health/ready`. **Próxima ordem — PRODUTO:** visão unificada de postura, exposições, vulnerabilidades, ativos e remediação, mantendo a postura NIST e o risco de exposição como indicadores DISTINTOS. Anterior: **AEGIS-MVP-OPS-01** — readiness recorrente barato / custo do Neon (PR #42, squash-merge `28e7d262aa125c7f994d67127314af3af6520b0c`; baseline anterior `859d5c3`; `main == origin/main == 28e7d26` por fast-forward; árvore idêntica ao head validado `c740a2b`; 2 commits `1616df8` (implementação e testes) + `c740a2b` (handoffs pós-PR #41); merge por `lipeadb-arch` em 2026-08-23T14:14:23Z; **SEM migration** — contexto principal permanece em **32 migrations**). **STATUS: CONCLUÍDO e mergeado.** **Correção do padrão caro de readiness:** o `/health/ready` (probe recorrente do Render, `healthCheckPath: /health/ready`) executava o `SchemaReadinessGuard` COMPLETO a cada probe — relia migrations dos dois contextos, catálogo, metodologia, regras, mapeamentos e proveniência do PostgreSQL externo (transferência recorrente compatível com o esgotamento da franquia do Neon de 5,43/5 GB). Agora a validação estrutural COMPLETA e **fail-closed** permanece no arranque, antes de `app.Run()` (`SchemaReadinessGuard.EnsureReadyAsync`); um **`StartupReadinessState`** singleton monotônico (latch `volatile`, sem banco/cache/tabela/migration/worker) é marcado pronto SOMENTE após `EnsureReadyAsync` retornar com sucesso; o readiness recorrente passa a checar só o estado de startup + `Database.CanConnectAsync` (conectividade leve); `/health/live` permanece independente do banco; Render segue em `/health/ready` (`autoDeploy: false` intocado). Schema/pacote inválido ainda **aborta o boot**; PostgreSQL inacessível ainda **reprova o readiness**; resposta HTTP **sanitizada**. **Evidências (validação proporcional — suíte de 978 NÃO repetida):** `HealthCheckTests` **8/8**; `SchemaReadinessGuardTests` **21/21**; builds da API e do projeto de testes **0 erros/0 avisos**; `has-pending-model-changes` limpo nos **dois** contextos; **`migrations-pg` verde**; **`container-smoke` verde** (migrator antes da API → PostgreSQL 18 descartável → seed/bootstrap → prontidão → `/health/ready` 200 pelo probe leve → `/health/live` 200 → SPA). **Sem deploy; Neon/Render NÃO tocados; nenhuma medição operacional.** ⚠️ **Correção técnica CONCLUÍDA, mas efeito operacional AINDA NÃO medido** — o padrão caro foi removido no código; a redução real de transferência só será confirmada após deploy autorizado e observação; NÃO declarar os 5,43 GB resolvidos, o ambiente público reativado, o deploy concluído ou Neon/Render validados, nem atribuir 100% da transferência ao readiness. **Próxima ordem: (1) disponibilidade/renovação da franquia do Neon; (2) deploy manual autorizado; (3) observação das métricas de transferência e frequência de conexões; (4) confirmação do efeito do hotfix e da estabilidade de `/health/ready`; (5) retomada do fluxo environment-first (menor exigência documental).** Anterior: **AEGIS-MVP-MULTICLOUD-01** — vulnerabilidades multicloud com Google Cloud (PR #41, squash-merge `859d5c3390d645a461cc384fead6b3aaf5aefb8e`; baseline anterior `a4167cc`; árvore idêntica ao head validado `ba8d060`; 3 commits técnicos `71c61ba`→`5931beb`→`ba8d060`; merge por `lipeadb-arch` em 2026-08-23T08:10:04Z; **SEM migration nova** — contexto principal permanece em **32 migrations**). **STATUS: CONCLUÍDO e mergeado.** **Google Cloud é a SEGUNDA fonte de Vulnerability Management** (primeira não-Microsoft), provando a fundação PROVIDER-NEUTRAL: conector `Google/VulnerabilityScanner` sobre **VM Manager / OS Config Vulnerability Reports** (`osconfig.googleapis.com`; escolhido em vez do Security Command Center por relação recurso×CVE nativa, escopo de PROJETO e permissão mínima somente leitura `roles/osconfig.vulnerabilityReportViewer`), coleta somente leitura de instâncias e vulnerabilidades por zonas explicitamente configuradas, normalizada no MESMO modelo do Defender (`AssetSourceBinding`/`AssetThreatObservation`/`AssetThreatExposure` + catálogo global de CVE) e reconciliada pelo MESMO `VulnerabilityReconciler` — **Microsoft + Google como demonstração multicloud de vulnerabilidades** (um Asset correlacionado, mesmo CVE → uma exposição, duas observações; lifecycle independente por fonte). **Boundary de autenticação fail-closed:** somente `service_account`; endpoint OAuth oficial FIXO por constante `https://oauth2.googleapis.com/token` (o `token_uri` do tenant é validado mas nunca é autoridade de destino; nenhum endpoint/credential source do tenant decide o destino); sem domain-wide delegation; `locations`/projeto/segredo/`AuthType` inválidos falham antes da coleta; logs e mensagens sanitizados. Score e vereditos DETERMINÍSTICOS; IA CONSULTIVA. **Evidências:** **67 testes direcionados**; backend local **978/978** (0 falhas, 0 ignorados); **Angular build aprovado**; **PostgreSQL 18 real no CI** (job `migrations-pg`, `AEGIS_PG_GATE_REQUIRED=true`) com **7 testes, 0 falhas, 0 pulados**; subset PDF Linux 11/0/0; **`container-smoke` verde**; **nenhuma migration nova / `has-pending-model-changes` limpo**. **Limitações de validação real:** transporte/parsing por **HTTP SIMULADO**, ainda **sem projeto Google Cloud real** (o fluxo OAuth real contra o endpoint oficial não foi chamado pelos testes); **AWS ainda sem conector**; **postura NATIVA do Google Cloud NÃO implementada**; **correlação automática entre IDs Microsoft×Google PENDENTE**; nenhum deploy/Render/Neon/tenant real tocado. ⚠️ **BLOQUEIO OPERACIONAL:** o **Neon Free atingiu 5,43/5 GB de transferência**; causa provável = **`/health/ready` executando o `SchemaReadinessGuard` COMPLETO a cada probe**. **Próxima ordem: (1) hotfix de readiness/consumo do Neon** (reduzir o custo do readiness sem enfraquecer o fail-fast de startup)**; (2) retomada da homologação pública; (3) fluxo environment-first com menor exigência documental; (4) visão unificada de postura, exposição e remediação.** Anterior: **AEGIS-MVP-VULN-01** — gestão multicloud de vulnerabilidades com Microsoft Defender (PR #40, squash-merge `a4167cc`; baseline anterior `30a1d56`; árvore idêntica ao head validado `97d49b9`; 3 commits técnicos `a354d9d`→`5659799`→`97d49b9`; merge por `lipeadb-arch` em 2026-08-23T02:18:09Z; contexto principal em **32 migrations**). **STATUS: CONCLUÍDO e mergeado.** Fundação PROVIDER-NEUTRAL/multicloud: `AssetSourceBinding` (identidade da máquina por FONTE — `machineId` só no binding, nunca em `Asset.ExternalRef`) + `AssetThreatObservation` (lifecycle Open/Resolved por CONECTOR) sobre a exposição CONSOLIDADA ativo×CVE (aberta enquanto qualquer fonte a observar; `ConnectorConfigId` é a autoridade, `SourceLabel` só apresentação); catálogo GLOBAL de CVE em `Threat` (severidade/CVSS/vetor/datas/publicExploit/exploitVerified/EPSS). **Microsoft Defender Vulnerability Management** é a PRIMEIRA fonte (conector `Microsoft/VulnerabilityScanner`, transporte `DefenderApiClient` fail-closed, três endpoints); API `GET /api/v1/vulnerabilities` + página `/vulnerabilities`. Score e vereditos DETERMINÍSTICOS (vulnerabilidade não gera `EvidenceSignal`/`TenantControlState` nem altera o AEGIS Score); IA CONSULTIVA (explicação/correlação/contextualização/priorização/apoio à remediação). Integridade TENANT-SAFE por FKs COMPOSTAS `(Id, TenantId)` nas cinco relações críticas (`exposição→ameaça` permanece simples: CVE global com `TenantId` nulo); migration `20260822220321_VulnerabilityManagement_MultiSourceFoundation` com guards fail-closed (exposição histórica de tenant divergente e CVE global duplicado). **Evidências:** backend local **911/911** (0 falhas, 0 ignorados); **PostgreSQL 18 real no CI** (job `migrations-pg`, `AEGIS_PG_GATE_REQUIRED=true` — sem falso verde) com **7 testes, 0 falhas, 0 pulados**, incluindo **1 `VulnerabilityManagementPostgresTests`** de verdade (sem `PULADO (local)`; migration aplicada; unicidade global de CVE; unicidade natural de binding; e **as cinco rejeições cross-tenant comprovadas por nome de constraint** no log do PostgreSQL); **`container-smoke` verde** (imagem, PostgreSQL, DbMigrator antes da API, migrations, seed, prontidão, bootstrap, health 200, não-root, SPA). **Dependências/limitações:** transporte/parsing Defender validados por **HTTP SIMULADO**, não contra tenant Defender real/licenciado; **AWS e Google Cloud ainda sem conectores** de vulnerabilidade; **correlação automática entre IDs de provedores distintos PENDENTE** (modelo já permite múltiplos bindings por Asset); nenhum deploy/Render/Neon/tenant real tocado. **Próxima decisão pendente: a DEFINIR e AUTORIZAR** — não presumir `VULN-02`, AWS, Google Cloud nem correlação automática. Anterior: **AEGIS-MVP-POSTURE-02** — Microsoft Secure Score real e exposições de configuração (PR #39, squash-merge `30a1d56`; baseline anterior `f39afc5`; árvore idêntica ao head validado `f61c940`; merge por `lipeadb-arch` em 2026-08-22T15:36:49Z; contexto principal em **31 migrations**). Coleta REAL somente leitura via Microsoft Graph v1.0 (`secureScores?$top=1` + `secureScoreControlProfiles` paginado; permissão mínima `SecurityEvents.Read.All`; host oficial fixo; `SampleScores`/Demo/Stub removidos); UMA fotografia por sincronização (`ICombinedEvidenceCollector`) para score e findings; exposições de configuração provider-neutral (`PostureExposureFinding`) com reconciliação fail-closed (upsert, preserva `FirstSeenAt`, resolve só em coleta completa, sem exclusão silenciosa) + tela `/exposures` e endpoint tenant-scoped somente leitura `GET /api/v1/posture/exposures` — recomendações de postura, NÃO CVEs/vulnerabilidades de ativo; completude fail-closed (206 recusado, JSON/estrutura malformados sanitizados, correspondência integral `controlScore`×perfil, score/timestamp/paginação validados); NIST e score DETERMINÍSTICOS (`NistSignalMapper`/regras/ledger); IA exclusivamente CONSULTIVA (sem autoridade sobre score, gap, rank ou ciclo de vida); migration `20260821232922_PostureExposureFindings`. **Evidências:** backend **851/851**; **PostgreSQL 18 real no CI** (job `migrations-pg`) com **6 testes, 0 falhas, 0 pulados** (incluindo exatamente 1 `PostureExposurePostgresTests`: migration aplicada confirmada por `GetAppliedMigrationsAsync` + reconciliação/idempotência/resolução + rejeição de chave natural duplicada); **`container-smoke` verde** (migrator→bootstrap→API, health/readiness 200, SPA, não-root). **Dependência externa:** validação futura contra um tenant Microsoft REAL ainda PENDENTE — o protocolo foi validado por HTTP SIMULADO; nenhum deploy/Render/Neon/tenant real tocado nesta rodada. **Próximo trabalho: a DEFINIR e AUTORIZAR** (sem escopo novo presumido; não presumir "POSTURE-03"). Anterior: **AEGIS-MVP-POSTURE-01** — fundação de catálogo, metodologia e proveniência (PR #38, squash-merge `f39afc5`; baseline `1f70eb4`; **30 migrations**). Separação explícita catálogo OFICIAL NIST × metodologia AUTORAL AEGIS × regras; proveniência versionada com histórico e hashes rederiváveis do banco (`ReferenceDataFingerprint`); `RuleEvidenceType` persistido como autoridade; seed determinístico fail-closed; exatamente uma framework ativa (índice parcial único); bundle SHA-256 nos snapshots. **Evidências:** backend SQLite **786/786**; **PostgreSQL 18 real no CI** (job `migrations-pg`) com **5 testes, 0 falhas, 0 pulados** (4 `PostureFoundationPostgresTests`); **`container-smoke` verde** (DbMigrator → migrations → seed → prontidão → API). IA sem autoridade sobre fórmula/pontos/cobertura; nenhum deploy/Render/Neon/tenant real tocado. Anterior: Fix do Secret File textual do Render (PFX em Base64 `.b64`, fail-closed, binário preservado) — PR #30, squash-merge `7a1d7af` (branch `fix/render-textual-pfx-secret` removida local e remota; homologação pública EM ANDAMENTO, Render `aegis-score` **SUSPENSO**). Anterior: Configurações, usuários, acessos e tenants — PR #29, squash-merge `28b61feb` (branch `feat/settings-user-access-management` removida local e remota). Página central `/settings` (Geral · Usuários e acessos · Integrações **reaproveitada**), onboarding exigindo PlatformAdmin+TenantAdmin, criação de tenant concedendo `TenantAdmin` ao criador, troca da própria senha ATÔMICA + rate-limited, guardas do último administrador (auto-desativação/auto-rebaixamento barrados, `FOR UPDATE` sob concorrência real, bypass da concessão/onboarding FECHADO); claim `has_local_credential`; **SEM migration**. Backend **672/672** (SQLite) + PostgreSQL focado do último admin **3/3**, `dotnet build -c Release` **0/0**, `ng build` aprovado, smoke autenticado, `container-smoke` verde. (Anterior: homologação em CONTAINER — PR #28 `ef4f42c`: imagem única Angular+API+DbMigrator, migrator antes da API, bootstrap opcional fail-closed do 1º admin, não-root, PKCS#12 montado, PostgreSQL externo TLS `VerifyFull`, sem migration. Antes: PDF/CSV `AEGIS-AUD-034` PR #27 `b9cbcba`; snapshots `AEGIS-AUD-035/036/037` PR #26 `dab5e6e`.)<br>
**Próximo trabalho:** MVP acelerado **CONCLUÍDO** (Entregas 0–5) e **fundação do AEGIS KNIGHT CONCLUÍDA** — PR #23 (squash-merge `d53beb3`; árvore idêntica ao head validado `1b59989`; branch `feat/aegis-knight-foundation` removida): assessment de identidade/exposição **SOMENTE demonstração**, catálogo original `ak-knight-v1` (`AK-ENTRA-001..005`), fórmula própria `knight-score-v1` **separada** do AEGIS Score, vereditos determinísticos, IA apenas consultiva com fallback, FK composta tenant/run + unicidade por indicador, migration `20260803140802_Knight_Foundation` (25ª); backend **559/559**, `dotnet build` **0/0**, `ng build` aprovado, **smoke funcional e visual** em PostgreSQL descartável (`aegis_dev` intocado; score 23 / cobertura 100% / 5 indicadores / `advisoryFromAi=false` / isolamento). **Microsoft Graph real, AD local, Okta, PDF e integrações externas ainda NÃO existem.** **Sequência acelerada do AEGIS KNIGHT** (sem criar AUDs individuais por indicador): **Entrega 2** — Microsoft Entra ID **real** + ~10–15 indicadores de alto valor; **Entrega 3** — relatório, histórico comparativo e exportação; **Entrega 4** — correlação de evidências SIEM/EDR; **Entrega 5** — AD local/híbrido, se o prazo permitir. **Entregas 1 e 2 do AEGIS KNIGHT CONCLUÍDAS. A Entrega 2 (coleta REAL do Microsoft Entra ID + arquitetura MULTICOLETOR) foi CONCLUÍDA — PR #24 squash-merge `ea38bd6` (árvore idêntica ao head validado `bc4f69c`; branch `feat/aegis-knight-multicollector-entra-live` removida local e remota): coletor Graph somente-leitura por client credentials, 15 indicadores `AK-ENTRA-001..015`, integração configurável pela interface (IdentityPosture, provider Microsoft=0 / capability=10, "Abrir AEGIS KNIGHT" em vez dos botões genéricos), avaliação estritamente FAIL-CLOSED (base de login/Graph constantes oficiais + `@odata.nextLink` validado — o bearer não vaza; paginação não truncada em silêncio; MFA vazio/malformado e coberturas incompletas de MFA/atividade de privilegiados → NotEvaluated; AK-ENTRA-010 mede permissões CONCEDIDAS e AK-ENTRA-013 consentimento DELEGADO tenant-wide, não `requiredResourceAccess`; Conditional Access só aprova cobertura global comprovada; estados reais de falha preservados; token malformado → AuthenticationFailure sanitizada), Demo preservado (score 23); migration `20260803190253_Knight_MultiCollector` (26ª), sem migration nova nas correções; `has-pending-model-changes` limpo; backend 585/585, `dotnet build` 0/0, `ng build` aprovado. Microsoft Graph validado por HTTP SIMULADO — AINDA NÃO contra um tenant Entra real; sem credenciais/segredos/dados reais. Coletor REAL Google Workspace somente leitura CONCLUÍDO — PR #25 (squash-merge `24e8e61`; árvore idêntica ao head `0d0f18d`; branch removida): catálogo `ak-knight-v2`, 6 indicadores `AK-GWS-001..006`, service account com domain-wide delegation (biblioteca oficial `Google.Apis.Auth`), escopos somente leitura Directory/Reports, host oficial fixo, paginação fail-closed, só metadados administrativos/auditoria; sem migration; testes focados KNIGHT+Google 51/51, `dotnet build` 0/0, `ng build` aprovado; Demo (score 23), Entra e AEGIS Score geral preservados; validado por HTTP SIMULADO, ainda não contra tenant Google real. Próximo trabalho técnico imediato: AEGIS KNIGHT — relatório executivo, histórico comparativo e exportação.** ⚠️ **Validação no notebook corporativo: PENDENTE / NÃO EXECUTADA** — sem evidência de portabilidade corporativa até o Felipe executar o roteiro nessa máquina

**Primeiro pacote pós-MVP CONCLUÍDO — snapshots auditáveis de postura (`AEGIS-AUD-035`, `AEGIS-AUD-036`, `AEGIS-AUD-037`): PR #26, squash-merge `dab5e6e`** (árvore idêntica ao head validado `47a5cd5`; branch `feat/aegis-auditable-history` removida local e remota). Fotografia IMUTÁVEL COMPARTILHADA entre AEGIS Score/NIST e AEGIS KNIGHT (sem score combinado), publicação controlada por papel **Manager/TenantAdmin**, universo NIST COMPLETO com "não avaliado" ≠ 0 e ≠ NonCompliant, proveniência da evidência DECISIVA, hash determinístico INEQUÍVOCO, imutabilidade reforçada por **gatilho append-only no PostgreSQL**, comparação só entre compatíveis (senão estado explícito de incompatibilidade) e tela compartilhada `/history` (histórico, detalhe e comparação). Migration ADITIVA `20260811143617_Auditable_Posture_Snapshots` (a **27ª**). O mesmo PR corrige a **corrida concorrente no upsert do ledger** (`ControlStateWriter`): recuperação pontual da inserção perdida, reaplicando o veredito como UPDATE sem duplicar (sem migration). Validação real: `dotnet build` **0/0**, backend **611/611**, **bateria PostgreSQL 20/20** (gatilho + corrida do ledger + `Concurrent_MesmoEvento` aprovado 3×), DbMigrator em banco descartável (`aegis_dev` intocado), `ng build` e **smoke visual** do histórico aprovados. **Exportação executiva PDF/CSV (`AEGIS-AUD-034`) CONCLUÍDA** — PR #27 (squash-merge `b9cbcba`; árvore idêntica ao head validado `957aecd`; branch `feat/aud-034-executive-export` removida): endpoint `GET /api/v1/posture/snapshots/{id}/export?format=pdf|csv` com PDF executivo pt-BR (PDFsharp-MigraDoc 6.2.0, edição CORE/MIT, sem GDI/Office) e CSV (UTF-8+BOM, `;`, protegido contra CSV/Formula Injection), ambos derivados EXCLUSIVAMENTE da `PostureSnapshot` imutável (tenant implícito, hash reverificado → 404/400/409); botões Baixar PDF/CSV no detalhe de `/history`; **sem migration**, sem mudança de fórmula/catálogo/ledger/Dashboard. Validação (árvore incorporada): `dotnet build` **0/0**, **backend 622/622** (+11 focados, incl. equivalência detalhe×PDF×CSV), `ng build` aprovado, `has-pending-model-changes` limpo (**27 migrations, nenhuma nova**; modelo EF sem mudança), smoke visual dos PDFs/CSVs. **`AEGIS-AUD-040` PARCIAL**: relatório da MESMA projeção publicada atendido; Dashboard ao vivo ligado a snapshot fica como resíduo (fora do escopo, **NÃO bloqueia a homologação**). **Risco operacional:** a geração de PDF exige fonte TTF instalada — validar no futuro container **Linux** (fonte instalada ou empacotada). **Próximo trabalho prioritário: implantação de homologação em nuvem e validação com tenant REAL (Microsoft Entra ID, depois ingestão genérica autenticada de SIEM/EDR); o resíduo do AUD-040 fica para depois, salvo bloqueador real.**

**Preparação para homologação em CONTAINER CONCLUÍDA — PR #28, squash-merge `ef4f42c`** (árvore idêntica ao head validado `fa82eb9`; branch `feat/cloud-homologation-readiness` removida local e remota). **Imagem única** de homologação: frontend Angular + API .NET 10 + `AegisScore.DbMigrator` na MESMA imagem (`Dockerfile` multi-stage, runtime ASP.NET 10 **não-root**), entrypoint fail-closed **migrator → API** (migrator antes da API). **Bootstrap OPCIONAL e fail-closed** do 1º **PlatformAdmin/TenantAdmin** no DbMigrator (idempotente; restrito ao primeiro administrador). **Data Protection com PKCS#12 montado** (usuário não-root no grupo 1000 lê o secret file); **PostgreSQL EXTERNO com TLS `SSL Mode=VerifyFull`**. `render.yaml` com variáveis sem valores reais; **Document Hub exige disco persistente** para sobreviver a redeploys. **SEM migration nova (27)**; sem mudança em score/NIST/KNIGHT/ledger/snapshots. **Workflow Linux `container-smoke` APROVADO** (run `31714307152` no head `fa82eb9`): **PDF 11/11**, **bootstrap 6/6**, **container smoke 11/11**, **backend 0/0**, **frontend aprovado**, **27 migrations (nenhuma nova)**. ⚠️ **Nenhum Neon ou Render provisionado; nenhuma implantação pública; nenhum tenant real conectado.** `AEGIS-AUD-056` **NÃO** deve ser declarado concluído apenas por este workflow focado. **Próximo passo (autorização/custo de Felipe):** (1) autorizar custo → (2) provisionar Neon → (3) provisionar Render → (4) configurar certificado e segredos → (5) ativar bootstrap só no 1º deploy → (6) validar o site → (7) desligar o bootstrap → (8) configurar App Registration e conectar o tenant real.

**Configurações, usuários, acessos e tenants CONCLUÍDO — PR #29, squash-merge `28b61feb`** (branch `feat/settings-user-access-management` removida local e remota). Entrega vertical de PROVISIONAMENTO/ADMINISTRAÇÃO sobre o bootstrap do PR #28: página central `/settings` com abas **Geral · Usuários e acessos · Integrações** (a de Integrações **REAPROVEITADA**, sem duplicar catálogo/form/segredos/KNIGHT; `tenantAdminGuard` nas rotas administrativas — visibilidade ≠ autorização). Endpoints: `GET /api/v1/users`; `POST /api/v1/platform/tenant-users` (onboarding ATÔMICO exigindo PlatformAdmin **E** TenantAdmin — identidade nova cria conta+acesso, existente preserva senha/`PlatformRole`/vínculo Entra); `PUT /api/v1/users/{id}` e `POST /api/v1/users/{id}/deactivate|reactivate` (devolvem `TenantUserDto` com `hasLocalCredential`, tenant-scoped, sem hash); `POST /api/v1/auth/password` (troca ATÔMICA + revogação de todas as sessões da identidade em todos os tenants, rate-limited `auth-password`); `POST /api/v1/tenants` concede `TenantAdmin` ao criador atomicamente. **Guardas do último administrador**: auto-desativação/auto-rebaixamento barrados; nunca zera o `TenantAdmin` ativo (correto sob concorrência real via `FOR UPDATE` no PostgreSQL); revogação de refresh tokens em desativação/rebaixamento; e o **bypass da concessão/onboarding de identidade existente FECHADO** (autoridade ÚNICA de mutação de membership; ator SEMPRE da claim `account_id`). Claim segura `has_local_credential` no access token. **SEM migration** (`has-pending-model-changes` limpo; 27 migrations). Validação (head `c726c13`, árvore idêntica ao squash): `dotnet build -c Release` **0/0**; backend **672/672** (SQLite) + **bateria PostgreSQL FOCADA do último admin 3/3** (incl. o caminho público de concessão/upsert, 5 iterações cada); `ng build` aprovado (só warnings de budget CSS conhecidos); `git diff --check` limpo; **smoke autenticado** (same-origin, PostgreSQL descartável, dados 100% sintéticos, `aegis_dev` intocado) e **workflow `container-smoke` APROVADO no head final `c726c13`**. ⚠️ Limitação AMBIENTAL conhecida: a bateria PostgreSQL COMPLETA em paralelo falha no teardown do `PostgresProbe` (usuário local sem `pg_signal_backend` para `DROP … WITH (FORCE)`) — é **teardown, não lógica**; a bateria focada passou isolada e a SQLite é 672/672; permissões do servidor NÃO alteradas. **Sem segredo/dado real; nenhum Neon/Render provisionado; nenhum tenant real conectado.** **Próxima ordem de trabalho: HOMOLOGAÇÃO PÚBLICA** — (1) provisionar o ambiente autorizado (Neon + Render), (2) executar o **bootstrap ÚNICO** e (3) desligá-lo em seguida (`Bootstrap__Enabled=false`), (4) criar o acesso do **gestor** pela nova aba Usuários e acessos, (5) configurar tenant/integração, (6) ingerir logs sintéticos ou autorizados e (7) preparar os resultados da demonstração. O passo-a-passo de infraestrutura do PR #28 (autorizar custo → Neon → Render → certificado/segredos → App Registration → conectar tenant real) permanece como dependência externa (autorização/custo de Felipe).

**Homologação pública EM ANDAMENTO — fix do Secret File textual CONCLUÍDO (PR #30, squash-merge `7a1d7af`; branch `fix/render-textual-pfx-secret` removida local e remota; árvore idêntica ao head validado `a0d9243`).** Neon Free (Oregon, PG18, `neondb`, endpoint direto, `SSL Mode=VerifyFull`) criado; Render Blueprint `aegis-homolog` + Web Service `aegis-score` (Starter US$ 7/mês) criado, mas o **1º deploy falhou e o serviço foi SUSPENSO**; a causa (apenas as aspas externas na connection string) foi corrigida na configuração EXTERNA do Render. **Bloqueador real corrigido no PR:** o Render só aceita Secret Files **textuais**, então o PFX binário não pode ser enviado — adicionado suporte **fail-closed** a PFX em **Base64 textual** (`.b64`), decodificado e carregado **em memória** pelo `DataProtectionPlan` (whitespace/quebras finais tolerados, chave privada exigida, falhas sanitizadas, nada de segredo em log/disco; senha SEPARADA; **binário preservado**). SEM mudança em auth/banco/score/NIST/KNIGHT/frontend/migrations/Neon; **SEM migration (27)**. Validação: `dotnet build -c Release` **0/0**, **testes de certificado 25/25**, `git diff --check` limpo, sem segredo no diff; **`container-smoke` APROVADO** (run `31959956298`) — provou o caminho `.b64` no container real (smoke 11/11, PDF Linux 11/11). **Nenhum tenant real conectado. Render `aegis-score` continua SUSPENSO.** Próximo: gerar o **Base64 LOCAL** do PFX → adicionar o Secret File `.b64` → **disco de 1 GB** + `DocumentStorage__RootPath=/var/aegis-data/document-store` → ativar o bootstrap **só no 1º deploy** → retomar o serviço → 1º login → desligar o bootstrap → smoke público.

**Pacote técnico pré-tenant — evidência documental rastreável, retração/reconciliação ATÔMICA, status ao vivo e terminologia CONCLUÍDO — PR #31, squash-merge `89f13f9e3c81f0e5353a866c4b162577efb5d3ca`** (árvore do squash IDÊNTICA ao head validado `0f55c90`, tree `58e096ae`; pai = baseline `7a1d7af`; `main == origin/main == 89f13f9e` por fast-forward; branch `fix/document-evidence-lifecycle-live-ux` removida local e remota; 44 arquivos, **28 migrations**). Corrige os defeitos vistos na homologação antes de conectar um tenant real: um documento sem prova literal deixava o **Govern em 40%** (o Stub fabricava GV.PO-01 "aprovada pela direção"; o worker projetava a triagem crua; o DELETE deixava cobertura/ledger órfãos; não havia status ao vivo). Agora **(A)** exige-se `evidenceQuote` LITERAL validado em código e projeta-se só resultados refinados+validados (refinamento indisponível → retry durável, nunca triagem-como-prova; documento sintético → zero claims; teto documental de 50%; **confiança < 0,70 mantém rastreabilidade/cobertura parcial mas NÃO concede crédito ao score**); **(B)** uma rotina ÚNICA de reconciliação (exclusão/reanálise) retrai/recalcula preservando telemetria e entrevista, com a migration `20260816192614_DocumentEvidenceLifecycle` (a **28ª**) + REPARO idempotente pelo `DbMigrator` que zera o Govern órfão no próximo deploy e re-enfileira documentos existentes; **(C)** o Document Hub faz polling (~2s, `timer`+`exhaustMap`, para no estado terminal) e atualiza os agregados na conclusão e na exclusão sem refresh; **(D)** terminologia visível padronizada (nomes de produto/NIST/códigos preservados). **Validação (head validado `0f55c90`):** `dotnet build -c Release` **0/0**, `has-pending-model-changes` limpo (**28 migrations**), backend focado **SQLite 80/80**, **`container-smoke` + `document-evidence-repair-pg` APROVADOS** (run `31983682757` — transição REAL da migration sobre **PostgreSQL 18** com dados legados, não banco vazio), `ng build` aprovado, `git diff --check` limpo, sem segredo. **Neon e Render NÃO alterados manualmente nesta sessão; nenhum tenant real conectado.** ⚠️ `DbMigrator` local NÃO executado nesta sessão (credencial PostgreSQL local inacessível ao agente) — coberto por `has-pending-model-changes`, `document-evidence-repair-pg` (PG18 real) e `container-smoke`. Próximo: deploy da nova `main` em homologação → confirmar migration aplicada, **Govern órfão 40% → Não avaliado**, upload **Na fila → Analisando → Analisado**, documento sem trecho probatório não altera a postura → prosseguir para o tenant real. PR: https://github.com/lipeadb-arch/AEGIS/pull/31 (merged).

**Correção pontual do dashboard executivo — ICR sintético removido + terminologia Synapse CONCLUÍDA — PR #32, squash-merge `fc2827a61a529a64c7ff9e698d582e54cb8446cf`** (árvore idêntica ao head validado `68554a2`; pai ÚNICO = baseline `89f13f9e`; `main == origin/main == fc2827a` por fast-forward; branch `fix/exec-dashboard-icr-and-synapse` removida local e remota; 7 arquivos técnicos; Neon e Render NÃO alterados; nenhum tenant real conectado). No tenant `AEGIS Homolog`, sem postura/evidência medida, o cabeçalho exibia `45 · ICR Moderado`: o `DashboardController` fabricava um proxy sintético de constantes (`TechnicalSeverity=0.5`, `AssetCriticality=0.5`, `RecentExploitation=0.3`, `RegulatoryExposure=0.4`…) que, com os pesos default e sem maturidade, caía exatamente em 45/Moderado, apresentado como postura apurada sem uma única medição. Removido integralmente o fallback/proxy; o contrato do dashboard executivo é **anulável de ponta a ponta** (`IcrDto?` no backend, `Icr | null` no frontend) e devolve `icr: null` quando não há nenhum `IcrScore`; cabeçalho e gauge passam a mostrar um estado neutro e explícito (`—` / `ICR · Não avaliado`, sem número/cor de banda/"Moderado"), preservando cliente e instante de apuração; ausência de medição não vira zero nem banda. Com `IcrScores` reais o comportamento é **inalterado — fórmula, pesos e faixas 40/60/80 do ICR intactos**. Removidas as referências visíveis ao **Synapse** em `frontend/src` (sidebar, cabeçalho, CSS órfão `.brand-sub`, comentários) — `rg -i synapse frontend/src` = 0. **SEM migration** (`has-pending-model-changes` limpo; modelo EF sem mudança). Validação (head `68554a2`): `dotnet build -c Release` **0/0**; testes focados **6/6** (`ExecutiveDashboardIcrTests`); **SQLite 688/688**; `ng build` aprovado (warnings de budget CSS conhecidos); `git diff --check` limpo. Nova baseline da `main` = `fc2827a`. ⚠️ A implantação automática no Render da nova `main` **ainda deve ser confirmada — sem evidência de que já ocorreu**. Próximo trabalho técnico: integridade da evidência documental, retração do score e atualização automática do Document Hub. PR: https://github.com/lipeadb-arch/AEGIS/pull/32 (merged).

**Pacote de IA FUNDAMENTADA (Gemini Free demonstrativo) CONCLUÍDO — PR #33, squash-merge `cd121d1e714b4be77aaa3c616b33b997a9d4b4e1`** (árvore idêntica ao head validado `8ff0fc5`, tree `fefce99`; pai = baseline `fc2827a`; `main == origin/main == cd121d1` por fast-forward; branch `feat/gemini-ai-grounded` removida local e remota; 43 arquivos, 2 commits `5a7325c` + `8ff0fc5`; **NÃO deployado; sem chave e sem tenant real**). Consolida a IA do AEGIS num provedor ÚNICO com configuração genérica `Ai__*` (Anthropic e a seção `AegisAi` FORA da DI ativa; `AegisAssessmentService` PROVIDER-NEUTRAL implementa `IAiAssessmentService` sobre o `ILLMClient`, com o `GeminiLlmClient` isolado na Infrastructure — nenhum tipo Gemini em controllers/workers/domínio/frontend). Gemini Free DEMONSTRATIVO com modelo estável pinado **`gemini-3.5-flash`** (GA + Free Tier; não alias `-latest`). **Score/pontos/ledger DETERMINÍSTICOS preservados** — a IA só interpreta/redige. **Fronteira de dados do Free Tier por CONFIGURAÇÃO, SEM migration e SEM tocar a entidade `Tenant`** (`Ai__Mode=GeminiFreeDemo` + `Ai__FreeTier__AllowedTenantSlugs__0` = slug do sandbox sintético): tenant fora da allowlist opera no motor determinístico/stub e NUNCA chama a IA externa. Gate + roteadores tenant-scoped protegem TODOS os consumidores (worker documental, Auditor, entrevistas, assessments, advisories, telemetria, Knight); 429/timeout/5xx SEM fallback simulado (nunca real→stub numa falha): telemetria mantém o veredito determinístico, documento faz retry→`Falha` (sem stub) e o Auditor informa; o middleware distingue **cota** (`ai_quota_exhausted`) de **indisponibilidade** (`ai_unavailable`) em 503 sanitizado. **Parecer documental** exibido no Document Hub (`DocumentMappingDto.EvidenceQuote` + `analysisSummary`, "Analisado com/sem evidência", citação literal + controle + confiança + justificativa; documento sem prova literal → NÃO altera a postura; polling mantido). **Auditor FUNDAMENTADO** num contexto tenant-scoped somente leitura (score/cobertura, lacunas, evidência curta, conectores, recomendações) — cita a origem, separa fato/inferência/recomendação e admite "não há dados suficientes"; nunca envia documento/log bruto. Endpoint `GET /api/v1/ai/status` — estado **`DemoConfigured`** = configurado, **não** health check em tempo real — + componente reutilizável de aviso ("só dados sintéticos ou demonstrativos"; nunca expõe a chave); o `GeminiLlmClient` não envia parâmetros de amostragem (só `maxOutputTokens`), conforme a doc do Gemini 3.x. **Validação:** `dotnet build -c Release` **0/0**; backend **704/704** (SQLite, exceto a bateria PostgreSQL) + **115/115** focados de IA/middleware após a 2ª correção; `ng build` aprovado (warnings de budget CSS conhecidos); `git diff --check` limpo; sem segredo no diff; **SEM migration (28)**. **PRs #31 e #32 preservados como CONCLUÍDOS. Nova baseline da `main` = `cd121d1`.** ⚠️ Render ainda NÃO recebeu o deploy da nova `main` nem a configuração externa; nenhuma chamada real ao Gemini foi executada até o merge. **Próximo trabalho operacional (nesta ordem):** (1) ROTACIONAR a credencial de demonstração exposta, sem registrar o valor; (2) deploy manual da nova `main` no Render + configuração externa; (3) criar/configurar a chave Gemini Free; (4) habilitar `Ai__Mode=GeminiFreeDemo` SOMENTE para o slug sintético autorizado; (5) smoke pequeno com dados exclusivamente fictícios; (6) então criar e conectar o tenant Entra de laboratório com dados sintéticos.

**Config: modo da IA administrado pelo painel do Render CONCLUÍDO — PR #34, squash-merge `2112fedaaf6230992031b7447e7035ecb1256555`** (árvore idêntica ao head validado `4f0f2ba`, tree `881ce73`; pai = `cd121d1`; `main == origin/main == 2112fed` por fast-forward; branch `fix/render-ai-mode-dashboard` removida local e remota; 3 arquivos: `render.yaml` + os 2 handoffs). `Ai__Mode` passou a **`sync: false`** no `render.yaml`: administrado NO PAINEL do Render, não versionado pelo Blueprint — sincronizações futuras do Blueprint **preservam** o modo operacional informado no painel (não retornam a IA a `Simulated`). `Ai__ApiKey` e a allowlist seguem `sync:false`; `Provider`/`Model`/`BaseUrl` inalterados. Nenhum segredo ou valor operacional versionado; **SEM código, migration, build ou testes** (alteração exclusiva de configuração declarativa). **Nova baseline da `main` = `2112fed`.** Próximo trabalho operacional: (1) confirmar a ROTAÇÃO da senha de demonstração exposta; (2) sincronizar/verificar o Blueprint do Render; (3) configurar a chave Gemini Free sem expô-la; (4) preencher a allowlist com o slug sintético exato; (5) `Ai__Mode` → `GeminiFreeDemo`; (6) deploy manual da nova `main`; (7) smoke mínimo com dados exclusivamente sintéticos.

**Fix do timeout da análise documental com Gemini CONCLUÍDO — PR #35, squash-merge `18b134bdacf5fde294fff0fd1466f5e7e62c7e54`** (árvore idêntica ao head validado `3025580`, tree `46f4ca2`; pai = `2112fed`; `main == origin/main == 18b134b` por fast-forward; branch `fix/gemini-document-timeout` removida local e remota; 10 arquivos: 4 backend + 3 testes + `render.yaml` + os 2 handoffs). **Falha real na homologação:** a análise documental falhava as 5 tentativas com `analysisError=TimeoutRejectedException` (o timeout por tentativa do Polly de 60s truncava a análise documental real do Gemini; a exceção não era traduzida). Correções: **timeout Gemini 60→120s** com o **Polly como autoridade única**; **timeout nativo do `HttpClient` desabilitado** (`Timeout=InfiniteTimeSpan`) só para o cliente Gemini; `GeminiLlmClient` traduz `TimeoutRejectedException` → `AiUnavailableException` (sanitizada; cancelamento do chamador preservado; sem fallback stub), que o frontend já traduz; **máx. 2 tentativas só no Render** (`DocumentAnalysisQueue__MaxAttempts=2`; default do código = 5; lease/heartbeat/backoff/schema intactos). Validação: `dotnet build -c Release` **0/0**; testes focados de IA/resiliência/DI **122/122**; `git diff --check` limpo; sem segredo; sem migration/ModelSnapshot; frontend intocado; **nenhuma chamada Gemini real na correção**. **Nova baseline da `main` = `18b134b`.** Próximo passo operacional: (1) sincronizar/verificar o Blueprint; (2) deploy da nova `main`; (3) confirmar o serviço `Live` no novo squash; (4) clicar em **Reanalisar UMA única vez** no documento que falhou; (5) acompanhar até o estado terminal; (6) validar o parecer e as citações literais; (7) parar se ainda ocorrer timeout, sem novo ciclo de tentativas manuais.

**Document Hub — evidência demonstrativa + deduplicação + DOCX CONCLUÍDO — PR #36, squash-merge `8c0181885c797a5709351c2beb6275eb96f788ca`** (árvore idêntica ao head validado `36b3e0c`, tree `9b9ad14`; pai = `18b134b`; `main == origin/main == 8c01818` por fast-forward; branch `fix/document-demo-evidence-docx` removida local e remota; 13 arquivos). PR #35 já implantado no Render (timeout resolvido). A homologação (IA em `GeminiFreeDemo`) revelou: PDF sem prova → `Analisado sem evidência`, zero controles (CORRETO); TXT sintético positivo com execução explícita de `PR.AA-05` recusado INDEVIDAMENTE no julgamento dirigido (o rótulo sintético era tratado como não-evidência) e julgado 2×; `.docx` sem extrator terminava em `NotSupportedException`. Correções: **(1)** SÓ no `GeminiFreeDemo` (sinalização por `AiMode`, sem slug), a triagem e o julgamento dirigido recebem o contexto de laboratório sintético autorizado — não recusam por rótulo "sintético/fictício/demo", mas AINDA exigem prova concreta (ação/data/escopo/responsável/registro), `EvidenceQuote` literal e o validador em código como autoridade final; produção intacta; **(2)** deduplicação dos candidatos (um por código NIST, maior confiança, ordem determinística) — `PR.AA-05` gera um único julgamento; **(3)** extrator **DOCX** (Open XML SDK, só leitura de parágrafos/tabelas, sem macro; `System.IO.Packaging` fixado em 10.0.0) + recusa CLARA de formato no upload (422 antes da fila) e frontend/backend alinhados a PDF/TXT/DOCX. IA sem autoridade sobre score/fórmula/ledger; **SEM migration**. Validação (não repetida no merge — árvores idênticas ao head validado): build **0/0**; testes focados IA+Documents **199/199**; `ng build` aprovado; `git diff --check` limpo; sem segredo. **Nova baseline `main` = `8c01818`; Render ainda na baseline anterior (`18b134b`) até novo deploy.** Próximo: (1) deploy da nova `main`; (2) confirmar `Live` no novo squash; (3) reanalisar uma vez o TXT positivo; (4) confirmar `PR.AA-05`, citação literal e parecer demonstrativo; (5) reanalisar uma vez o DOCX antes rejeitado; (6) parar e reportar antes de iniciar o tenant Entra.

**Redefinição administrativa de senha local (recuperação legítima) CONCLUÍDO — PR #37, squash-merge `1f70eb4c90d414ce8886afb7f18378ee751cf1e9`** (árvore idêntica ao HEAD validado `1cb9c65`, tree `5f2004bb`; pai = `8c01818`; `main == origin/main == 1f70eb4` por fast-forward; branch `fix/platform-admin-password-reset` removida local e remota; 2 commits, 19 arquivos). **Nova baseline `main` = `1f70eb4`; deploy no Render ainda pendente.** **Causa:** a identidade GLOBAL (`IdentityAccount`: e-mail + `PasswordHash` + `PlatformRole`) é separada do membership (`User`, tenant-scoped); desativar/remover o `User` NÃO remove a `IdentityAccount`, e reconceder acesso ao mesmo e-mail PRESERVA a credencial global de propósito (fecha o takeover por TenantAdmin). Faltava, porém, recuperação legítima: havia a troca da PRÓPRIA senha (`POST /api/v1/auth/password`, que exige a senha atual), mas nenhuma redefinição administrativa para quem NÃO consegue mais autenticar — informar senha no onboarding de identidade existente é (corretamente) ignorado e excluir/recriar membership não resolve. **Correção (sem e-mail/SMTP, sem fluxo anônimo):** rota GLOBAL `POST /api/v1/platform/identities/{accountId}/password` (`PlatformIdentitiesController`), protegida SÓ pela policy de plataforma (nenhum `TenantAdmin` — gate por papel de tenant seria takeover cross-tenant); alvo é a `IdentityAccount` (nunca membership). Reusa `PasswordPolicy` (NIST 12–128) e `IPasswordHasher`; inexistente → **404 genérico**; federated-only (`PasswordHash==null`) → **409** sem criar credencial local; auto-redefinição por essa rota → **409** (usar a troca normal); sucesso → **204**, substitui o hash e **revoga todos os refresh tokens da identidade em TODOS os tenants** ATOMICAMENTE — bloqueia a RENOVAÇÃO das sessões no ato, mas um access token já emitido segue válido até seu teto de ~10 min (núcleo sensível `AuthService.ReplaceHashAndRevokeAllSessionsAsync` refatorado e COMPARTILHADO com a troca da própria senha; falha na revogação reverte o hash novo — sem estado parcial). Rate limiting `platform-password-reset`; auditoria SÓ com `actorAccountId`/`targetAccountId`/nº de ambientes (nunca senha/hash/e-mail). A listagem tenant-scoped passa a expor `identityAccountId` (`TenantUserDto`) para o front chamar a rota global — sem conceder autoridade, isolamento preservado. **Frontend (Configurações → Usuários e acessos):** botão **Redefinir senha** só quando PlatformAdmin + alvo com credencial local + não é a própria conta (predicado puro); modal (nova senha + confirmação 12–128) explicando que a identidade GLOBAL muda e a RENOVAÇÃO das sessões é bloqueada em todos os ambientes (acessos já emitidos valem até 10 min) — DIFERENTE de desativar; sucesso sem exibir a senha; conta federada → "Senha administrada pelo provedor corporativo"; onboarding orienta a usar Redefinir senha (não excluir/recriar); senha nunca em signal/storage/log/console. Validação: `dotnet build -c Release` **0/0**; testes focados de autenticação/administração (SQLite) **73/73** (`AdminPasswordResetTests` + contrato + autorização por reflexão); `ng build` aprovado (só warnings de budget CSS conhecidos); `git diff --check` limpo; sem segredo; **SEM migration** (não exige coluna nova). ⚠️ Frontend sem runner de teste no repo (validação de front = `ng build`); o requisito de visibilidade fica no predicado puro type-checked + spec `*.spec.ts` (roda sob `ng test` quando um runner existir). SEM alteração em scoring/Document Hub/Entra/migrations/Neon/Render/dados reais; nenhuma chamada Gemini. Próximo passo operacional: (1) publicar/deploy da nova `main` (`1f70eb4`) no Render — pendente; (2) redefinir a credencial do usuário afetado por esta rota (canal seguro, sem registrar o valor) e **somente então** testar o novo login; (3) retomar o smoke documental do PR #36 (reanalisar UMA vez o TXT e o DOCX) e, em seguida, a preparação do tenant Entra de laboratório com dados sintéticos.

> Este plano não exige mais concluir os 63 achados antes de apresentar o produto. O objetivo imediato é
> entregar um **MVP funcional, demonstrável e pronto para homologação**, preservando segurança
> multi-tenant e integridade da pontuação. Os demais achados permanecem registrados para o pós-MVP.

---

## 1. Decisão executiva

O processo anterior tratava cada `AEGIS-AUD-*` como um projeto independente, com investigação extensa,
PR isolado e repetição de toda a bateria de testes. Esse modelo é seguro, mas incompatível com o prazo de
um mês.

A partir desta revisão:

- o trabalho será organizado por **fluxos verticais de produto**, não por um AUD por vez;
- até cinco PRs técnicos devem encerrar o MVP após o PR #16;
- um PR pode resolver vários achados relacionados;
- investigação deixa de ser uma fase separada: deve ser curta e ocorrer dentro da implementação;
- testes serão proporcionais ao risco e não repetidos em SHAs inalterados;
- documentação será limitada aos dois handoffs existentes, depois de cada merge;
- achados que não bloqueiam a demonstração, a segurança do tenant ou a integridade do score ficam
  formalmente **ADIADOS PARA PÓS-MVP**.

O alvo desta revisão não é uma produção MSSP completa com HA, DR, LGPD, supply chain e todos os
fornecedores certificados. O alvo é um produto coerente e funcional que possa ser instalado, conectado a
um tenant de homologação, receber evidências, calcular postura e apresentar os resultados.

---

## 2. O que significa “AEGIS funcional e apresentável”

Até 2026-08-28, deve ser possível executar um roteiro ponta a ponta:

1. Preparar o PostgreSQL pelo `AegisScore.DbMigrator` e iniciar API e frontend.
2. Criar ou usar um tenant AEGIS, autenticar uma identidade e selecionar o ambiente correto.
3. Configurar uma integração sem persistir segredo em claro.
4. Receber eventos de SIEM/EDR por uma entrada autenticada e neutra de fornecedor.
5. Persistir a evidência recebida, sua origem, horário, integridade e mapeamento NIST.
6. Atualizar os controles e o AEGIS Score por regras determinísticas.
7. Diferenciar claramente controle **não avaliado**, **conforme** e **não conforme**.
8. Exibir Dashboard executivo com score, cobertura, controles críticos, conectores e recência dos dados.
9. Navegar pelas seis Funções do NIST CSF 2.0:
   - Governar — GV;
   - Identificar — ID;
   - Proteger — PR;
   - Detectar — DE;
   - Responder — RS;
   - Recuperar — RC.
10. Em cada Função, apresentar conteúdo, controles, estado, evidências e checklist de pendências.
11. Usar o Document Hub para upload, processamento, listagem, cobertura e consulta de documentos.
12. Trocar de tenant sem manter dados, cache ou indicadores do tenant anterior.

### Critério de honestidade do produto

- Integração real deve ser identificada como real.
- Stub, payload sintético ou adaptador ainda não implementado deve ser identificado como demonstração.
- A tela de Integrações não pode declarar um fornecedor “operacional” se não existir adaptador funcional.
- Ausência de evidência não pode virar nota zero nem conformidade.
- Falha da API não pode ser substituída silenciosamente por dados de demonstração.

---

## 3. Estado técnico aproveitável

Não é necessário reconstruir o projeto. A fundação existente deve ser reutilizada.

| Capacidade | Estado atual |
|---|---|
| .NET 10 / EF Core 10 / PostgreSQL | Concluído |
| Migrations externas ao startup e readiness de schema | Concluído |
| Segredos de conectores cifrados e key ring persistente | Concluído |
| Filas de documento e sync duráveis | Concluído |
| Escritas cross-tenant protegidas | Concluído |
| Refresh tokens persistidos somente por hash | Concluído |
| Login local, federação Entra e tenant switch | Implementados; falta fechar a experiência |
| Identidade global e membership por tenant | Concluído |
| Papéis globais e tenant-scoped | Concluído (PR #16) |
| Catálogo NIST CSF 2.0 | Existente |
| Document Hub | Existente e integrado ao backend; requer aceite ponta a ponta |
| Páginas GV, ID, PR, DE, RS e RC | Estrutura existente; conteúdo/estado ainda desigual |
| Configuração de conectores | Existente com segredos cifrados |
| Coleta real de SIEM/EDR | Incompleta; adaptadores atuais não comprovam operação real |
| Evidência normalizada e score | Parcial; autoridade e rastreabilidade precisam ser fechadas |
| Dashboard executivo | Existente; projeções e semântica ainda precisam ser unificadas |
| Testes backend | 537/537 na main (`9db7ba4`) |
| Frontend | Build aprovado; suíte ampla fica fora do MVP |

### Achados já concluídos

| ID | PR | Merge |
|---|---:|---|
| AEGIS-AUD-053 | #5 | `49a6747` |
| AEGIS-AUD-052 | #6 | `0ebad27` |
| AEGIS-AUD-057 | #7 | `9904729` |
| AEGIS-AUD-046 | #8 | `f9a3ed7` |
| AEGIS-AUD-050 | #9 | `f170b0f` |
| AEGIS-AUD-026 | #10 | `383bf6b` |
| AEGIS-AUD-031 | #11 | `d02cfee` |
| AEGIS-AUD-008 | #12 | `2f8c968` |
| AEGIS-AUD-009 | #13 | `37d57ff` |
| AEGIS-AUD-007 | #14 | `ff5d119` |
| AEGIS-AUD-010 | #15 | `d947328` |
| AEGIS-AUD-011 | #16 | `00937e9` |
| AEGIS-AUD-012 | #17 | `2fbc0d9` (squash) |
| AEGIS-AUD-018 | #17 | `2fbc0d9` (squash) |
| AEGIS-AUD-030 | #17 | `2fbc0d9` (squash) |
| AEGIS-AUD-020 | #18 | `c24c547` (squash) |
| AEGIS-AUD-041 | #18 | `c24c547` (squash) |
| AEGIS-AUD-043 | #18 | `c24c547` (squash) |
| AEGIS-AUD-001 | #19 | `c8ecb3f` (squash) |
| AEGIS-AUD-002 | #19 | `c8ecb3f` (squash) |
| AEGIS-AUD-019 | #19 | `c8ecb3f` (squash) |
| AEGIS-AUD-021 | #20 | `9db7ba4` (squash) |
| AEGIS-AUD-027 | #20 | `9db7ba4` (squash) |
| AEGIS-AUD-032 | #20 | `9db7ba4` (squash) |
| AEGIS-AUD-048 | #21 | `5a2f7a8` (squash) |

As **Entregas 1, 2, 3, 4 e 5** estão **CONCLUÍDAS** — Entrega 1 (fluxo de tenant confiável: `AEGIS-AUD-012`, `AEGIS-AUD-018`, `AEGIS-AUD-030`) em PR #17 (squash-merge `2fbc0d9`), Entrega 2 (ingestão operacional de evidências: `AEGIS-AUD-020`, `AEGIS-AUD-041`, `AEGIS-AUD-043`) em PR #18 (squash-merge `c24c547`), Entrega 3 (score determinístico e explicável: `AEGIS-AUD-001`, `AEGIS-AUD-002`, `AEGIS-AUD-019`) em PR #19 (squash-merge `c8ecb3f`) Entrega 4 (Workspace NIST, Dashboard e Document Hub: `AEGIS-AUD-021`, `AEGIS-AUD-027`, `AEGIS-AUD-032`) em PR #20 (squash-merge `9db7ba4`) e Entrega 5 (release candidate demonstrável: `AEGIS-AUD-048`) em PR #21 (squash-merge `5a2f7a8`). O **caminho crítico do MVP acelerado está CONCLUÍDO**; o que segue é o backlog pós-MVP (seção 6), sem novo AUD autorizado.

#### Fundação multicloud do Google SecOps (AEGIS-MVP-GOOGLE-SECOPS-01) — CONCLUÍDA (2026-08-30)

**PR #56** — squash-merge `2ad3315` (pai único `24e5509`; `main == origin/main == 2ad3315`; árvore idêntica ao head validado `5e0e631`; branch removida, local e remota). Entrega a **fundação SIEM provider-neutral** (duas dimensões independentes — casos e alertas —, estados explícitos Available/Partial/Unsupported/PermissionDenied/Throttled/Timeout/Unavailable, semântica de período RollingWindow × CurrentInventory, contagens anuláveis) e um **conector Google SecOps/Chronicle somente leitura operacional** (só HTTP GET: `instances.get`, `cases.list` v1 com agregação incremental, `legacySearchEnterpriseWideAlerts` em janela de 30 dias com parcialidade explícita). **Microsoft Sentinel preservado** sob o contrato neutro. Autenticação Google compartilhada por service account (**sem Domain-Wide Delegation**; scope `chronicle`; token URI oficial); hosts `*-chronicle.googleapis.com` de allowlist oficial, redirects recusados, bearer restrito ao host oficial, segredo nunca retornado/registrado, limites de corpo/páginas/itens e detecção de ciclo. **Casos e alertas NÃO são persistidos; ZERO `EvidenceSignal` e ZERO alteração do AEGIS Score; nenhuma mudança de catálogo, metodologia, regras ou mappings NIST** (score determinístico e IA consultiva intocados).

**Evidências (CI do PR #56):** etapa direcionada `Google SecOps and Sentinel connector tests on Linux` no job `container-smoke` — **108 executados / 0 falhas / 0 pulados** (HTTP simulado, em Linux); **`container-smoke` verde** (imagem Docker; DbMigrator antes da API; bootstrap/seed; `/health/ready` e `/health/live` 200; SPA; execução não-root); **`migrations-pg` verde — PostgreSQL 18 real, 8 executados / 0 falhas / 0 pulados**. Nenhuma migration nova.

⚠️ **Limitação (API viva):** a **validação contra uma instância Google SecOps real permanece PENDENTE** — sem credenciais/ambiente habilitado; o contrato oficial está coberto **apenas por testes HTTP simulados**.

**Próxima ordem (`AEGIS-MVP-GOOGLE-SECOPS-02`):**
1. **validação controlada contra uma instância Google SecOps real**, quando houver credenciais autorizadas;
2. `Google SecOps → MITRE ATT&CK`;
3. evidência determinística `MITRE ATT&CK → NIST CSF 2.0`;
4. **nenhuma pontuação baseada apenas em volume de alertas**.

#### Guarda de score: alerta SIEM não concede conformidade (AEGIS-MVP-SCORE-GUARD-SIEM-01) — CONCLUÍDA (2026-08-31)

**PR #57** — squash-merge `1fff7bd` (pai único `2ad3315`; `main == origin/main == 1fff7bd`; árvore idêntica ao head validado `f568f42`; branch removida, local e remota; 4 commits técnicos — `45f5ba2`, `f4176be`, `7f9b267`, `f568f42`). **Defeito corrigido:** o mapping `(Siem, siem.alert.highSeverity → DE.AE-02/DE.CM-01)` concedia `Compliant`/pontos/cobertura pela mera presença de alerta. **Correção:** mapping **aposentado** (removido dos defaults e removido de forma idempotente no seed, só o par exato) e **recusado pelo readiness** (invariante fail-closed no `SchemaReadinessGuard`); **push futuro fica fail-closed** (`siem.alert.highSeverity` → 422 Unmapped). O alerta permanece **fato operacional/consultivo**, nunca evidência positiva de controle. **Reparo legado** (recompute pela autoridade única `EvidenceTelemetryRecompute`): **retração conservadora para "Não avaliado"** (nunca NonCompliant), **reprojeção por evidência válida remanescente**, **isolamento entre tenants**, **preservação do sinal legado**, **idempotência**, **sem migration nova**.

**Evidências (CI do PR #57):** **`migrations-pg` — PostgreSQL 18 real, 12 executados / 0 falhas / 0 pulados**, incluindo os quatro cenários `ScoreGuardSiemRepairPostgresTests` (remoção do mapping no pacote real + prontidão; retração idempotente; reprojeção de DE.CM-01 por EDR provada por contraste + A/B; isolamento tenant-scoped); **`container-smoke` verde**; **Google SecOps/Sentinel preservados** (108/0/0). **Score, fórmula e pesos determinísticos preservados; IA consultiva; presença ou volume de alertas não é evidência positiva de controle.** Nenhum deploy; Neon/Render não tocados.

#### Cobertura de detecção MITRE no Google SecOps (AEGIS-MVP-GOOGLE-SECOPS-02) — CONCLUÍDA (2026-09-02)

**PR #58** — squash-merge `c61256b` (`merged_at` 2026-09-02T01:47:30Z; **pai único `1fff7bd`**; `main == origin/main == c61256b`; **árvore idêntica ao head validado `ebe5184`** — tree `54481a5`; branch `feat/mvp-google-secops-02-detection-coverage` removida, local e remota). Entrega a **cobertura de detecção provider-neutral** (regras do SIEM × **MITRE ATT&CK Enterprise v17.1**) na área Detect, **estritamente CONSULTIVA**. Transporte `rules.list` em **`CONFIG_ONLY`** (paginação/ciclo/tetos/redirect/sanitização, reusando o conector Google existente). **Parser MITRE** dos formatos oficiais: case-insensitive com normalização ao ID canônico só após validar; em `tags` só o namespace `google.mitre.technique` (tag curta + resource name), ignora `google.mitre.tactic`, não extrai `T####` avulso; em metadata só chaves oficiais `technique`/`mitre_ttp`. **`executionState` modelado como dimensão própria** (`DEFAULT`/`LIMITED`/`PAUSED`/desconhecido), particionando as regras em live mode — no snapshot e por técnica; **linguagem operacional honesta** (execução normal só com live + `DEFAULT`; nunca afirma que alertas foram produzidos). **Persistência agregada e tenant-safe:** entidades `DetectionCoverageSnapshot`+`DetectionCoverageTechnique`, migration `20260901005858_DetectionCoverage_GoogleSecOps`, substituição atômica + fingerprint + preservação em falha, **FK composta `(ConnectorConfigId, TenantId)` → `ConnectorConfig (Id, TenantId)` OnDelete Cascade** (rejeita órfão/cross-tenant, cascateia na exclusão do conector). **Nunca persiste nome/texto/conteúdo de regra. ZERO `EvidenceSignal`, ZERO `TenantControlState`, ZERO alteração do AEGIS Score/NIST; IA estritamente consultiva.**

**Evidências (CI do PR #58, head `ebe5184`):** **`migrations-pg` — PostgreSQL 18 real, 14 executados / 0 falhas / 0 pulados** (inclui os 2 Facts de `DetectionCoveragePostgresTests` — substituição atômica/unicidade/isolamento e FK tenant-safe/cascade); **`container-smoke` verde**. Migration aplicada em PostgreSQL 18; `has-pending-model-changes` limpo. Nenhum deploy; Neon/Render não tocados.

⚠️ **Limitação (API viva):** a **validação da leitura da Rules API contra uma instância Google SecOps real permanece PENDENTE** — sem credenciais/ambiente habilitado; coberto apenas por transportes HTTP simulados.

**Próximo pacote — PRIORIDADE DEFINIDA POR FELIPE (2026-09-05): consolidação do produto em três dias.** O foco deixa de ser abrir novas superfícies de coleta e passa a ser a **coerência e a prontidão do que já existe** — dashboard executivo, relatório inteligente, navegação e linguagem operacional —, de modo que o AEGIS seja demonstrável de ponta a ponta com os conectores já entregues. A fila anterior fica **subordinada** a essa consolidação: (1) concessão de `IdentityRiskyUser.Read.All` e `IdentityRiskEvent.Read.All` + validação viva do Identity Risk; (2) validação viva do Intune após `DeviceManagementManagedDevices.Read.All`; (3) Defender Unified RBAC; (4) Microsoft 365 Activity Feed × Google SecOps; (5) validação real do Google SecOps quando o Service Account JSON estiver disponível. Nenhum desses itens é iniciado antes da consolidação, e os que dependem de credencial seguem bloqueados por autorização externa.

---

## 4. Caminho crítico de 30 dias

### Visão geral

| Ordem | Entrega vertical | AUDs prioritários | Prazo-alvo | Resultado visível |
|---:|---|---|---|---|
| 0 | Fechar separação de papéis | AEGIS-AUD-011 | ✅ Concluída | PR #16 mergeado (`00937e9`); autoridade global e tenant separadas |
| 1 | Fluxo de tenant confiável | AEGIS-AUD-012, AEGIS-AUD-018, AEGIS-AUD-030 | ✅ Concluída | PR #17 mergeado (`2fbc0d9`); login/seleção/switch sem retenção cross-tenant |
| 2 | Ingestão operacional de evidências | AEGIS-AUD-020, AEGIS-AUD-041, AEGIS-AUD-043 | ✅ Concluída | PR #18 mergeado (`c24c547`); ingestão genérica autenticada SIEM/EDR, evidência persistida e mapeada para NIST |
| 3 | Score determinístico e explicável | AEGIS-AUD-001, AEGIS-AUD-002, AEGIS-AUD-019 | ✅ Concluída | PR #19 mergeado (`c8ecb3f`); score reproduzível `aegis-score-v1`, IA consultiva, cobertura ≠ score |
| 4 | Workspace NIST, Dashboard e Hub | AEGIS-AUD-021, AEGIS-AUD-027, AEGIS-AUD-032 | ✅ Concluída | PR #20 mergeado (`9db7ba4`); projeção única `/scoring/workspace`, seis Funções equivalentes, Dashboard AEGIS Score, saúde honesta dos conectores |
| 5 | Release candidate demonstrável | AEGIS-AUD-048 + correções bloqueadoras | ✅ Concluída | PR #21 mergeado (`5a2f7a8`); `/health/live`+`/health/ready`, senha demo só em runtime, smoke `scripts/smoke-mvp.ps1` (35/35), correção do `<base href>`, roteiro em `DEV.md` |

Os IDs agrupam riscos já catalogados, mas o aceite é pelo **fluxo funcionando**, não por quantidade de
AUDs encerrados.

### Entrega 0 — concluir o PR #16 — ✅ CONCLUÍDA

**Status:** CONCLUÍDA — PR #16 squash-merge `00937e9`; `main` local/remota sincronizadas; branch removida.<br>
**Escopo:** finalizar o AEGIS-AUD-011 já implementado.<br>
**Fora do escopo:** reabrir arquitetura de identidade ou executar AUD-012 no mesmo PR.

Aceite:

- `TenantRole` e `PlatformRole` separados;
- backfill de `PlatformAdmin` não reativa membership inativo ou tenant suspenso;
- migration, constraints e testes PostgreSQL aprovados;
- merge e handoff concluídos.

### Entrega 1 — fluxo de tenant confiável

**Status:** ✅ CONCLUÍDA — PR #17 (squash-merge `2fbc0d9`); `main` local/remota sincronizadas; branch `feat/mvp-tenant-flow` removida.<br>
**AUDs:** AEGIS-AUD-012, AEGIS-AUD-018 e AEGIS-AUD-030 em um único PR.

Implementar apenas o necessário para:

- selecionar explicitamente um tenant quando houver mais de um acesso;
- usar último tenant somente após revalidar membership e status;
- rejeitar `X-Tenant` ausente, inválido ou divergente quando a rota exigir tenant;
- no switch, limpar estado e requisições do tenant anterior antes de carregar o novo;
- recarregar Dashboard, Hub, páginas NIST, integrações e indicadores;
- impedir que resposta atrasada do tenant anterior repovoe a UI;
- manter o login local e federado existentes.

Não criar um novo sistema de sessão nem ampliar o SLA de revogação neste pacote.

**Aceite (evidência):** login/troca federada com desfecho explícito — recusa (0 acessos) · seleção automática (1) · seleção explícita ou último tenant **revalidado** (vários), via **ticket curto purpose-bound** em `POST /auth/select-tenant`; `X-Tenant` **fail-closed** (ausente/vazio/malformado → 400; divergente ou token sem tenant → 403; família `/auth` isenta); no switch, o **cancelamento das leituras do tenant anterior precede a troca**, que envia o **Bearer local + o `X-Tenant` atual** e recarrega o novo tenant **sem resposta atrasada**. Backend **462/462**; `ng build` aprovado (4 warnings CSS conhecidos); **sem migration, schema ou credenciais**. Login local/federado, refresh e `TenantRole`×`PlatformRole` preservados.

### Entrega 2 — ingestão operacional de evidências

**Status:** ✅ CONCLUÍDA — PR #18 (squash-merge `c24c547`); `main` local/remota sincronizadas; branch `feat/mvp-evidence-ingestion` removida.<br>
**AUDs:** AEGIS-AUD-020, AEGIS-AUD-041 e AEGIS-AUD-043 em um único PR.

Objetivo: tornar o AEGIS capaz de **receber dados reais sem depender de um adaptador específico**.

Escopo mínimo:

- contrato normalizado e versionado para eventos de SIEM e EDR;
- entrada autenticada por credencial própria do conector, isolada por tenant;
- idempotência por identificador ou hash do evento;
- persistência do payload bruto protegido, origem, tipo, instante de coleta e instante de recebimento;
- executor único para coleta/push e atualização de `ConnectorConfig.LastSyncAt/LastStatus`;
- uma autoridade central de mapeamento `sinal → subcategorias NIST`;
- rejeição explícita de sinal sem mapeamento, sem pedir ao LLM para inventar um;
- endpoint/teste de conexão e estado visível na tela de Integrações;
- payloads de referência para pelo menos um formato SIEM e um formato EDR;
- marcação clara dos fornecedores que ainda não possuem adaptador real.

O caminho genérico autenticado é o requisito do MVP. Adaptadores completos para Sentinel, Splunk,
CrowdStrike, Google SecOps e outros podem ser adicionados depois sem bloquear a entrega.

**Aceite (evidência):** endpoint **`POST /api/v1/ingestion/connectors/{connectorId}/events`** autenticado por **`X-Ingestion-Key`** (persistida só como **hash SHA-256**, comparação em tempo constante); **tenant derivado exclusivamente do `ConnectorConfig` autenticado** (contrato v1 sem `TenantId`/score/veredito do cliente); respostas **400/401/422/200** + **rate limiting**; **idempotência como invariante de banco** (índice único parcial; concorrência real em PostgreSQL — 8 requisições → 1 persistência); **RawPayload protegido** (Data Protection, purpose próprio); **`SignalMapping` como única autoridade** de mapeamento, **sem LLM** no caminho; **executor único push/pull** para Generic SIEM e Generic EDR, com **adaptadores de fabricantes honestamente marcados como não implementados**; amostras `samples/ingestion` só com dados reservados/sintéticos. Migration **`20260730184530_Aud020GenericEvidenceIngestion`** (**24 migrations**); **sete `SignalMappings`**. Backend **472/472**; `AegisScore.DbMigrator --verify-only`/`has-pending-model-changes` aprovados; **smoke real PostgreSQL** aprovado (SIEM, EDR, deduplicação, chave inválida, sinal desconhecido, proteção do payload, ausência de LLM). **Sem credenciais, segredos ou dados reais.** Adaptadores reais de fabricantes permanecem fora desta entrega.

### Entrega 3 — score determinístico e explicável

**Status:** ✅ CONCLUÍDA — PR #19 (squash-merge `c8ecb3f`); `main` local/remota sincronizadas; branch `feat/mvp-deterministic-score` removida.<br>
**AUDs:** AEGIS-AUD-001, AEGIS-AUD-002 e AEGIS-AUD-019 em um único PR.

Escopo mínimo:

- uma fórmula oficial, simples, versionada e usada pelo backend;
- estados distintos para `NotEvaluated`, `Compliant` e `NonCompliant`;
- denominador calculado apenas sobre o universo semântico definido pela fórmula;
- telemetria e regra determinística como autoridades do veredito;
- IA limitada a resumo, explicação e recomendação;
- todo score rastreável aos controles e evidências que o compõem;
- motivo legível quando um controle não pontua;
- checklist derivado de controles sem evidência ou não conformes, sem criar um subsistema de workflow.

Não implementar neste MVP campanhas complexas, confiança estatística da IA ou fórmulas alternativas.

**Aceite (evidência):** fórmula oficial **`aegis-score-v1`** como autoridade ÚNICA de pontos, arredondamento e estado; **score ANULÁVEL** (0/0 = `NotEvaluated`, nunca 0%) e **cobertura** (peso avaliado / peso elegível do catálogo ativo) como eixos distintos; **agregação compartilhada** (`AegisScoreAggregator`) entre **Score Atual e snapshot diário**, restrita ao **framework ATIVO** — estado de versão antiga não entra no score nem infla a cobertura (avaliado ≤ elegível). Veredito determinístico por **telemetria + `SignalMapping.ScoringHint`** (`DeterministicControlEvaluator`/`EvidenceSignalEvaluator`); **IA limitada a resumo/explicação/recomendação — não decide conformidade**. Projeção da evidência no ledger com **recompute GLOBAL "from-newest" entre conectores** (SIEM+EDR no mesmo controle; evento antigo não sobrescreve o mais novo; empate exato de instante → pior veredito conservador, depois chave/Id estáveis; isolamento por tenant, sem `IgnoreQueryFilters`) e **falha de projeção NÃO mascarada** (carimba `LastStatus=Failed` e propaga; o retry deduplicado reprojeta sem duplicar `EvidenceSignal`). O hint **`percent.higherIsBetter.v1` é BINÁRIO** (≥ 80% → Compliant, **< 80% → NonCompliant**; valor ausente/NaN/infinito/fora de 0–100 ou unidade incompatível → sem veredito) — **`MitigatedByThirdParty` reservado a controle compensatório real** (terceiro/MSSP ou isolamento OT no `DeterministicControlEvaluator`), com crédito de 50% preservado na fórmula. Dashboard catalog-first com `NotEvaluated`; frontend exibe "Não avaliado" e cobertura (percentual anulável). **Validação:** backend **528/528**; **`dotnet build` 0 erros / 0 warnings**; **`ng build` aprovado** com os **4 warnings de budget CSS conhecidos**; **`has-pending-model-changes` limpo — SEM migration nova** (a coluna `ScoringHint` já existia; a Entrega 3 apenas passou o **seed idempotente** a preenchê-la); `AegisScore.DbMigrator` (migrate+seed+verify e `--verify-only`) aprovado, **24 migrations**, **sete `SignalMappings` com ScoringHint conhecido**; **smoke real PostgreSQL** aprovado (ingestão SIEM/EDR, mapping+hint, ledger determinístico, score/cobertura, deduplicação, ordenação global entre conectores e retry/reprojeção; base descartável, `aegis_dev` preservado com os sete mappings). **Sem credenciais, segredos ou dados reais.**

### Entrega 4 — Workspace NIST, Dashboard e Document Hub

**Status:** ✅ CONCLUÍDA — PR #20 (squash-merge `9db7ba4`); `main` local/remota sincronizadas; branch `feat/mvp-nist-workspace` removida.<br>
**AUDs:** AEGIS-AUD-021, AEGIS-AUD-027 e AEGIS-AUD-032 em um único PR.

Escopo mínimo:

- uma projeção única para score atual, cobertura, contagem de controles e severidades;
- Dashboard executivo consumindo essa projeção;
- todas as seis Funções com padrão visual e funcional equivalente:
  - descrição e objetivo;
  - score/estado da Função;
  - cobertura de evidências;
  - lista de controles;
  - evidência mais recente;
  - pendências/checklist;
  - estados loading, vazio e erro;
- Govern mantém o Document Hub como área especializada;
- Identify mantém inventário/risco como área especializada;
- PR, DE, RS e RC usam o painel comum já existente;
- Hub com upload, fila/status de processamento, documentos, cobertura e erros compreensíveis;
- Dashboard exibe saúde/recência dos conectores e não apenas uma nota;
- tendência só é exibida quando houver dados semanticamente comparáveis.

Relatórios exportáveis, snapshots imutáveis e histórico regulatório ficam para o pós-MVP.

**Aceite (evidência):** **projeção compartilhada** `GET /api/v1/scoring/workspace` (`WorkspacePostureQuery`, pela fórmula `aegis-score-v1` sobre o framework ATIVO): postura geral e por Função (score anulável + cobertura + contagens + severidade + evidência mais recente) e saúde/recência dos conectores — a MESMA autoridade do Dashboard e das seis Funções, sem recomputo concorrente. **Dashboard** consome AEGIS Score, cobertura, contagens e saúde dos conectores via `PostureSummaryComponent` (banda distinta da **maturidade CMMI/ICR**, rotulada à parte); removida a contagem concorrente `IneffectiveControls`. **Seis Funções** com postura equivalente pelo componente compartilhado (PR/DE/RS/RC no painel comum, GV no Document Hub, **ID no Inventário + seção comum de controles/checklist `ID.*`**); `NotEvaluated` NUNCA vira 0%. **Document Hub** (upload/fila/status/cobertura/erro sanitizado/vazio) completo e reaproveitado, com a **postura GV recarregada após ações** (sync/upload/reanálise/remoção/cobertura). **Saúde dos conectores** operacional só entre **habilitados** (nunca-sincronizado ≠ saudável; desabilitado fora do denominador; expõe configured/enabled/disabled; Dashboard mostra `healthy/enabled`). **Estados** loading/vazio/erro + **retry** em todas as Funções; sem fallback de demonstração; troca de tenant cancela leituras + recarrega (reuso do `TenantContextService`). **Sem migration** (`has-pending-model-changes` limpo; 24 migrations). Backend **537/537** (+9 testes focados da projeção); `dotnet build` 0 erros/0 warnings; `ng build` aprovado (4 warnings de budget CSS conhecidos). ⚠️ O **smoke autenticado das seis rotas** não foi executado (rotas atrás de `authGuard`, sem credenciais no ambiente) — o boot em `/login` foi verificado sem erro de console; a cobertura E2E autenticada fica para a **Entrega 5**. **Sem credenciais, segredos ou dados reais.**

### Entrega 5 — release candidate demonstrável

**Status:** ✅ CONCLUÍDA — PR #21 (squash-merge `5a2f7a8`); `main` local/remota sincronizadas; branch `chore/mvp-release-candidate` removida (local e remota).<br>
**AUD principal:** AEGIS-AUD-048.

Escopo:

- health check de liveness sem dependências externas;
- readiness de PostgreSQL, migrations e dependências indispensáveis;
- configuração de Development/demonstração sem segredo versionado;
- smoke test do roteiro completo com dados sintéticos;
- validação de um tenant e troca entre dois tenants;
- validação de ingestão SIEM e EDR pelo contrato genérico;
- validação das seis Funções, Dashboard, Integrações e Hub;
- correção apenas dos bloqueadores encontrados no smoke;
- roteiro curto de instalação e demonstração usando documentação existente.

Essa entrega não inclui observabilidade distribuída, HA, disaster recovery ou hardening completo.

**Aceite (evidência):** PR #21 (squash-merge `5a2f7a8`, árvore idêntica ao head validado `c646565`; branch removida). **health checks** `GET /health/live` (liveness — não toca banco/IA/fornecedor) e `GET /health/ready` (readiness — PostgreSQL + migrations + catálogo/regras pelo `SchemaReadinessGuard`, só leitura; ausência de Azure OpenAI/Entra/conectores NÃO reprova), **anônimos** e com **resposta sanitizada** (só status; sem connection string/stack/detalhe). **Segredo `Aegis@12345` eliminado**: `seed-user` exige `Demo:Password` **em runtime** (env `Demo__Password`/user-secret; sem padrão versionado, nunca logada, não devolvida), e-mail demo `analista@demo.example.com`, restrito a DEBUG+Development; novo `seed-second-tenant` para o 2º ambiente. **Smoke** `scripts/smoke-mvp.ps1` (PowerShell 5.1+, banco descartável isolado, nunca toca `aegis_dev`, segredos só em runtime) — **35/35 gates**: DbMigrator, liveness/readiness, seeds sem segredo, login→seleção→troca entre dois tenants, `X-Tenant` fail-closed (403), conectores Generic SIEM/EDR, ingestão SIEM/EDR + dedup + chave inválida (401), score determinístico, seis Funções, Dashboard, Integrações, Hub, isolamento do 2º tenant. **Bloqueador corrigido:** `index.html` sem `<base href="/">` (tela branca em refresh/deep-link de rota aninhada). **Validação visual** das telas essenciais sem tela quebrada nem erro de console. Backend **540/540** (+3 health checks); `dotnet build` 0/0; `has-pending-model-changes` limpo — **sem migration** (24 migrations); `ng build` aprovado. Roteiro curto em `DEV.md`. **Sem credenciais, segredos ou dados reais.**

---

## 5. Política reduzida de testes

Testes existentes não devem ser apagados. A redução ocorre na repetição das baterias e na criação de
testes novos.

### Durante a implementação

- executar somente build e testes diretamente relacionados ao código alterado;
- não rodar a suíte completa após cada edição ou commit;
- não repetir teste em SHA inalterado;
- não criar testes para comentários, DTOs triviais ou getters sem lógica;
- limitar novos testes, em regra, a 3–8 cenários de maior risco por entrega.

### Antes do merge

| Tipo de mudança | Gate mínimo |
|---|---|
| Somente frontend | `ng build` + smoke dos fluxos alterados |
| Backend sem schema/segurança | build + testes direcionados + suíte completa uma vez |
| Migration, tenant, autenticação ou score | testes direcionados + PostgreSQL real focado + suíte completa uma vez |
| Documentação/handoff | `git diff --check`; sem build repetido |

### Release candidate

Executar uma única bateria consolidada:

- backend completo;
- testes PostgreSQL indispensáveis;
- frontend build;
- smoke E2E do roteiro de demonstração;
- verificação de isolamento entre dois tenants;
- verificação de que nenhum segredo ou dado real entrou no repositório.

Ficam adiados:

- suíte frontend ampla do AEGIS-AUD-033;
- matriz extensa de browsers;
- carga, caos, múltiplas réplicas e testes de DR;
- testes duplicados que provam a mesma invariante em várias camadas.

---

## 6. Backlog pós-MVP

Os achados abaixo não foram descartados. Eles deixam de bloquear a entrega de 30 dias.

### Avaliação, IA e refinamentos de domínio

`AEGIS-AUD-003`, `AEGIS-AUD-004`, `AEGIS-AUD-005`, `AEGIS-AUD-006`,
`AEGIS-AUD-025`, `AEGIS-AUD-044`, `AEGIS-AUD-045`.

### Arquitetura e hardening de identidade

`AEGIS-AUD-013`, `AEGIS-AUD-014`, `AEGIS-AUD-015`, `AEGIS-AUD-016`,
`AEGIS-AUD-017`.

### Dashboard, relatórios e histórico avançado

`AEGIS-AUD-022`, `AEGIS-AUD-024`, `AEGIS-AUD-034`, `AEGIS-AUD-038`,
`AEGIS-AUD-039`, `AEGIS-AUD-040`. (`AEGIS-AUD-035/036/037` CONCLUÍDOS no PR #26 — snapshots auditáveis; `AEGIS-AUD-034` **CONCLUÍDO** no PR #27 (squash `b9cbcba`) — exportação executiva PDF/CSV a partir do snapshot imutável; `AEGIS-AUD-040` **PARCIAL** — relatório derivado da mesma projeção publicada atendido, vínculo do Dashboard ao vivo com snapshot é resíduo (não bloqueia homologação).)

### Neutralidade, UX e extensibilidade adicionais

`AEGIS-AUD-023`, `AEGIS-AUD-028`, `AEGIS-AUD-029`, `AEGIS-AUD-042`,
`AEGIS-AUD-047`.

### Qualidade e operação em escala

`AEGIS-AUD-033`, `AEGIS-AUD-049`, `AEGIS-AUD-051`, `AEGIS-AUD-054`,
`AEGIS-AUD-055`, `AEGIS-AUD-056`, `AEGIS-AUD-058`.

### Produção, privacidade e continuidade

`AEGIS-AUD-059`, `AEGIS-AUD-060`, `AEGIS-AUD-061`, `AEGIS-AUD-062`,
`AEGIS-AUD-063`.

Essa classificação cobre todos os achados não concluídos que ficaram fora do caminho crítico. Um item
pós-MVP só volta ao plano de 30 dias se surgir como bloqueador comprovado de segurança, integridade dos
dados ou funcionamento do roteiro de demonstração.

---

## 7. Regras de execução para ganhar velocidade

1. No máximo cinco PRs após o PR #16.
2. Um PR entrega um fluxo vertical utilizável; não um AUD isolado.
3. Leitura inicial deve se limitar aos arquivos diretamente envolvidos.
4. Não produzir relatório de investigação separado antes de implementar.
5. Não pedir autorização intermediária para decisões reversíveis dentro do escopo aprovado.
6. Parar somente diante de:
   - risco de perda de dados;
   - segredo real;
   - exposição cross-tenant;
   - migration sem caminho seguro;
   - divergência material de escopo.
7. Não abrir PR exclusivamente documental.
8. Atualizar `AEGIS_STATE.md` e este plano somente após o merge, de forma curta.
9. Não reescrever histórico válido nem repetir testes já associados ao mesmo SHA.
10. Se um refinamento não muda o roteiro de demonstração, movê-lo para o pós-MVP.

---

## 8. Gate de aceite do MVP

O MVP está concluído quando todos os itens abaixo forem demonstrados no mesmo ambiente:

- [ ] banco preparado e API pronta;
- [ ] login funcional;
- [ ] seleção e troca de tenant sem vazamento de estado;
- [ ] conector configurado com segredo protegido;
- [ ] ingestão autenticada de exemplo SIEM;
- [ ] ingestão autenticada de exemplo EDR;
- [ ] evidências persistidas e visíveis;
- [ ] mapeamento NIST determinístico;
- [ ] score reproduzível e explicável;
- [ ] não avaliado diferente de zero;
- [ ] Dashboard com score, cobertura, riscos, recência e saúde dos conectores;
- [ ] GV, ID, PR, DE, RS e RC com conteúdo, controles e checklist;
- [ ] Document Hub com upload, processamento, cobertura e erro visível;
- [ ] frontend sem fallback silencioso para demo;
- [ ] smoke test completo aprovado;
- [ ] nenhum segredo ou dado identificável versionado.

### Dependências externas que não são defeito de código

- App Registration e consentimento do Microsoft Entra ID para login federado real;
- credenciais de um SIEM/EDR real, se for desejado validar um adaptador específico;
- conectividade do notebook corporativo com PostgreSQL, APIs externas e os endpoints configurados.

Sem essas credenciais, o aceite de código usa o contrato genérico autenticado e payloads sintéticos. O
repositório nunca deve receber credenciais reais.

---

## 9. Limite da entrega

Ao final dos 30 dias, o AEGIS pode ser declarado:

**“MVP funcional e pronto para demonstração/homologação controlada.”**

Ainda não deve ser declarado:

**“Plataforma MSSP pronta para produção irrestrita.”**

Produção completa continuará dependendo do backlog pós-MVP, especialmente observabilidade, CI/CD,
hardening, privacidade, retenção, múltiplas réplicas e continuidade.

---

## 10. Uso com Claude e Codex

Ao iniciar uma sessão:

1. Ler `AEGIS_STATE.md` e este plano.
2. Confirmar branch, SHA e alterações locais.
3. Trabalhar na próxima **entrega vertical** da seção 4.
4. Investigar somente o necessário para implementar.
5. Priorizar código funcionando, integração e UX.
6. Aplicar a política reduzida de testes da seção 5.
7. Parar antes do merge para revisão.
8. Após o merge, atualizar somente os dois handoffs existentes.

Nenhum agente deve voltar automaticamente ao modelo antigo de executar os 63 achados em sequência.

---

**Fim do Plano Diretor de Remediação v1.0.3**
